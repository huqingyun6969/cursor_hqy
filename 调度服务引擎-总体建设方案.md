# 调度服务引擎 — 总体建设方案

> **版本**: v1.0  
> **日期**: 2026-03-03  
> **文档性质**: 总体方案（含功能架构 + 技术架构 + 对外调用方案）

---

## 目录

- [第一篇 方案概述](#第一篇-方案概述)
- [第二篇 功能架构](#第二篇-功能架构)
- [第三篇 技术架构](#第三篇-技术架构)
- [第四篇 对外调用方案](#第四篇-对外调用方案)
- [第五篇 数据库设计](#第五篇-数据库设计)
- [第六篇 部署与运维](#第六篇-部署与运维)
- [附录](#附录)

---

# 第一篇 方案概述

## 1.1 项目背景

视联平台需要一套 **可视化工作流编排与发布引擎**，使业务人员能够在 Web 端通过拖拽、连线完成 API 接口的编排，编排完成后一键发布为标准 RESTful API，自动注册到微服务中心，通过 **视联平台网关** 和 **能力开放网关** 双入口对外提供服务。类似"能力上架到视联平台"的模式——编排即开发、发布即上架。

## 1.2 系统定位

| 维度 | 说明 |
|------|------|
| 底层引擎 | FlowGram.AI（字节跳动开源的可视化工作流/智能体编排平台） |
| 项目形态 | 智能体全生命周期管理 + 可视化工作流编辑 + 任务运行时 + **流程发布** + **动态路由注册** + 双网关对外服务 |
| 核心价值 | 零代码编排 → 一键发布 → 自动注册 → 网关可调 → 调用可观测 |

## 1.3 总体架构概览

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              调用方                                             │
│  ┌──────────────┐   ┌──────────────────┐   ┌────────────────────────────────┐  │
│  │  视联平台前端  │   │  第三方业务后台    │   │  能力开放平台（订阅 & 调试）     │  │
│  └──────┬───────┘   └────────┬─────────┘   └───────────────┬────────────────┘  │
│         │                    │                              │                   │
└─────────┼────────────────────┼──────────────────────────────┼───────────────────┘
          │                    │                              │
          ▼                    ▼                              ▼
┌──────────────────┐  ┌───────────────────────┐  ┌──────────────────────────────┐
│  视联平台网关     │  │   统一 API Gateway     │  │   能力开放网关                │
│  (SceneCode 鉴权) │  │   (Nacos 服务发现)     │  │   (AccessKey 签名鉴权)        │
└────────┬─────────┘  └───────────┬───────────┘  └─────────────┬────────────────┘
         │                        │                             │
         └────────────┬───────────┘─────────────────────────────┘
                      │   Feign / LoadBalancer (Nacos)
                      ▼
         ┌────────────────────────────────────────┐
         │     smart-flow-engine (Spring Boot)     │
         │     已注册到 Nacos 微服务中心            │
         │  ┌──────────────────────────────────┐  │
         │  │  动态路由层                       │  │
         │  │  POST /smart/api/flow/{flowCode}  │  │
         │  └──────────────┬───────────────────┘  │
         │                 │                       │
         │  ┌──────────────▼───────────────────┐  │
         │  │  流程调度引擎                     │  │
         │  │  - 参数校验 & 类型转换            │  │
         │  │  - Schema 加载 & 缓存             │  │
         │  │  - FlowGram Runtime 执行          │  │
         │  │  - 结果组装 & 返回                │  │
         │  └──────────────────────────────────┘  │
         │                                         │
         │  ┌──────────────────────────────────┐  │
         │  │  发布管理模块                     │  │
         │  │  - 参数提取 & 类型推断            │  │
         │  │  - API 元数据注册                 │  │
         │  │  - 动态路由注册/注销              │  │
         │  │  - 版本管理 & 灰度                │  │
         │  └──────────────────────────────────┘  │
         └──────────────────┬──────────────────────┘
                            │
              ┌─────────────┼─────────────────┐
              ▼             ▼                 ▼
         ┌────────┐   ┌─────────┐   ┌─────────────────┐
         │  MySQL  │   │  Nacos  │   │  agents-server   │
         │ (元数据) │   │ (注册)  │   │  (FlowGram Node) │
         └────────┘   └─────────┘   └─────────────────┘
```

---

# 第二篇 功能架构

## 2.1 功能域划分

| 功能域 | 说明 | 主要入口 |
|--------|------|----------|
| **智能体管理** | 智能体的 CRUD、搜索、复制、基本信息编辑 | `/manage` 智能体管理页 |
| **工作流编辑** | 画布编辑、节点/连线、表单配置、变量、保存 | `/editor/:id` 编辑器页 |
| **任务执行与运行时** | 启动任务、轮询状态/结果、取消、校验 | 编辑器内「运行」、API |
| **流程发布与上架** | 参数提取、API 定义、动态路由注册、能力上架 | 编辑器「发布」、管理页「发布管理」 |
| **运行日志与执行记录** | 按智能体查看执行记录、状态筛选、详情 | 管理页「运行日志」弹窗 |
| **能力市场与订阅** | 已发布能力浏览、订阅、在线调试 | `/marketplace` 能力市场页 |
| **多端调用** | 前端代理、服务端运行时、Java HTTP、网关调用 | 配置与客户端 SDK |

## 2.2 用户角色与使用场景

| 角色 | 典型场景 |
|------|----------|
| **业务编排人员** | 在管理页创建智能体，进入编辑器设计工作流，保存后点击发布，填写 API 元信息 |
| **平台管理员** | 审核发布申请、管理能力上下架、查看调用统计 |
| **能力订阅方** | 在能力市场浏览已上架的流程能力，查看 API 文档，在线调试，获取调用凭证 |
| **第三方开发者** | 通过网关 HTTP 调用已发布的流程能力 API |
| **运维人员** | 查看运行日志、监控调用量、处理异常告警 |

## 2.3 智能体管理功能清单

| 序号 | 功能项 | 描述 | 对应 API/页面 |
|------|--------|------|----------------|
| F-A1 | 智能体列表展示 | 分页表格展示智能体（ID、名称、描述、发布状态、创建/更新时间） | `GET /api/agents`，`/manage` |
| F-A2 | 智能体搜索 | 按名称或描述关键词搜索 | `GET /api/agents?keyword=` |
| F-A3 | 分页与每页条数 | 支持页码、每页条数（10/20/50/100） | `page`, `pageSize` |
| F-A4 | 新增智能体 | 弹窗填写名称、描述；创建后列表刷新 | `POST /api/agents` |
| F-A5 | 编辑智能体基本信息 | 弹窗修改名称、描述 | `PUT /api/agents/:id` |
| F-A6 | 删除智能体 | 二次确认后删除 | `DELETE /api/agents/:id` |
| F-A7 | 复制智能体 | 复制为新智能体（名称带「_副本」），含 design_json | `POST /api/agents/:id/copy` |
| F-A8 | 进入设计 | 跳转到编辑器页 | 路由 `/editor/:id` |
| F-A9 | 查看运行日志 | 执行记录弹窗，支持分页与状态筛选 | `GET /api/agents/:id/executions` |

## 2.4 工作流编辑功能清单

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-B1 | 画布加载 | 根据智能体 ID 加载 design_json 到画布 |
| F-B2 | 节点类型支持 | 开始、结束、LLM、HTTP、代码、变量、条件、循环、块开始/结束、注释、分组 |
| F-B3 | 拖拽添加节点 | 从节点面板拖拽到画布 |
| F-B4 | 连线 | 端口间连线，环检测 |
| F-B5 | 删除连线/节点 | 快捷键 Backspace/Delete |
| F-B6 | 节点表单配置 | 点击节点在侧边栏配置 |
| F-B7 | 变量面板 | 全局变量配置 |
| F-B8 | 画布缩放与平移 | 缩放、平移、适应视口 |
| F-B9 | 缩略图 | 画布概览 |
| F-B10 | 对齐与辅助线 | 拖拽对齐辅助 |
| F-B11 | 撤销/重做 | 历史记录 |
| F-B12 | 保存 | 防抖后 PUT 更新 design_json |
| F-B13 | **测试运行** | 编辑器内填写 inputs 进行测试运行，查看输入输出结果 |
| F-B14 | **发布** | 编辑器工具栏「发布」按钮，触发发布流程 |

## 2.5 任务执行与运行时功能清单

| 序号 | 功能项 | 描述 | 对应 API |
|------|--------|------|----------|
| F-C1 | 启动任务 | 服务端接收 schema + inputs，调用 runtime-js 启动 | `POST /api/task/run` |
| F-C2 | 轮询任务状态 | 获取节点执行状态、日志 | `GET /api/task/report?taskID=` |
| F-C3 | 获取任务结果 | 获取 outputs 等最终结果 | `GET /api/task/result?taskID=` |
| F-C4 | 取消任务 | 取消正在运行的任务 | `PUT /api/task/cancel` |
| F-C5 | 校验工作流 | 校验 schema 及 inputs 合法性 | `POST /api/task/validate` |
| F-C6 | 执行记录落库 | 写入 task_executions 表 | 内部逻辑 |
| F-C7 | 内存清理 | 定时清理任务报告缓存 | `POST /api/task/cleanup` |

## 2.6 流程发布与上架功能清单（新增）

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-P1 | 参数自动提取 | 从 design_json 的 start 节点提取 inputs 参数定义 |
| F-P2 | 类型自动推断 | 根据默认值自动推断参数类型（String/Integer/Long/Double/Boolean/Object/Array） |
| F-P3 | 参数元数据编辑 | 编辑参数名、类型、是否必填、默认值、描述说明 |
| F-P4 | 响应结构定义 | 定义或录入响应 Body 的 JSON 结构体及字段说明 |
| F-P5 | API 基本信息 | 填写能力名称、描述、版本号、分类标签 |
| F-P6 | 路由地址生成 | 自动生成 `/smart/api/flow/{flowCode}`，支持自定义 flowCode |
| F-P7 | 鉴权方式选择 | 选择 SceneCode（视联平台网关）或 AccessKey（能力开放网关）或两者都支持 |
| F-P8 | 发布确认 | 校验必填项 → 注册动态路由 → 写入发布记录 → 注册 API 元数据 |
| F-P9 | 下架/停用 | 注销动态路由，保留发布记录但标记为已下架 |
| F-P10 | 版本管理 | 支持多版本发布，可按版本号灰度切换 |
| F-P11 | 发布审批（可选） | 发布前可配置审批流程 |

## 2.7 能力市场与订阅功能清单（新增）

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-M1 | 能力列表浏览 | 按分类、标签、关键词搜索已上架能力 |
| F-M2 | 能力详情页 | 展示 API 接口信息、参数说明、响应说明、鉴权方式、使用示例 |
| F-M3 | 在线调试 | 在能力详情页填写参数，直接发起测试调用 |
| F-M4 | 调用凭证管理 | 申请/查看 SceneCode 或 AccessKey |
| F-M5 | 调用统计 | 查看订阅能力的调用量、成功率、平均耗时 |

---

# 第三篇 技术架构

## 3.1 整体技术栈

| 层次 | 技术选型 | 说明 |
|------|----------|------|
| **前端** | React + TypeScript + Rsbuild | 智能体管理 + 工作流编辑器 + 能力市场 |
| **编辑器引擎** | @flowgram.ai/free-layout-editor | 自由布局画布、节点引擎、变量引擎、插件体系 |
| **运行时（Node.js）** | @flowgram.ai/runtime-js + Express | 工作流执行引擎，提供 run/report/result/cancel/validate |
| **发布与路由服务（Java）** | Spring Boot 2.7+ / Spring Cloud | 动态路由注册、API 元数据管理、Nacos 注册 |
| **微服务注册** | Nacos 2.x | 服务注册与发现、配置中心 |
| **API 网关** | 视联平台网关 + 能力开放网关 | 双入口，Feign/LoadBalancer 调用后端服务 |
| **数据库** | MySQL 8.0 | 智能体、发布记录、API 元数据、执行记录 |
| **缓存** | Redis 6.x | Schema 缓存、路由缓存、调用限流计数 |

## 3.2 系统分层架构

```
┌───────────────────────────────────────────────────────────────┐
│                        接入层 (Gateway)                       │
│  视联平台网关(SceneCode)    能力开放网关(AccessKey签名)         │
│  → Nacos 服务发现 → Feign 调用 smart-flow-engine             │
└───────────────────────────────┬───────────────────────────────┘
                                │
┌───────────────────────────────▼───────────────────────────────┐
│                   smart-flow-engine (Spring Boot)              │
│ ┌─────────────┐ ┌──────────────┐ ┌─────────────────────────┐ │
│ │ 动态路由控制器│ │ 发布管理服务  │ │ API 元数据服务           │ │
│ │ DynamicRoute │ │ PublishSvc   │ │ ApiMetaSvc              │ │
│ │ Controller   │ │              │ │                         │ │
│ └──────┬──────┘ └──────┬───────┘ └────────────┬────────────┘ │
│        │               │                      │               │
│ ┌──────▼───────────────▼──────────────────────▼────────────┐ │
│ │                   流程调度引擎核心                         │ │
│ │  - 参数校验 & 类型转换                                    │ │
│ │  - Schema 加载（DB + Redis 缓存）                         │ │
│ │  - 调用 agents-server（FlowGram Runtime）执行             │ │
│ │  - 结果组装 & 统一响应                                    │ │
│ └──────────────────────┬───────────────────────────────────┘ │
└────────────────────────┼─────────────────────────────────────┘
                         │ HTTP (RestTemplate / WebClient)
┌────────────────────────▼─────────────────────────────────────┐
│               agents-server (Node.js + Express)               │
│  FlowGram runtime-js 执行引擎                                 │
│  - POST /api/task/run     启动工作流                          │
│  - GET  /api/task/report  获取中间状态                        │
│  - GET  /api/task/result  获取最终结果                        │
│  - PUT  /api/task/cancel  取消任务                            │
│  - POST /api/task/validate 校验                               │
└──────────────────────────────────────────────────────────────┘
```

## 3.3 Spring Boot 动态路由设计

### 3.3.1 设计目标

流程发布后，无需重启服务即可注册新的 HTTP 端点，外部通过统一路径模式调用。

### 3.3.2 路由模式

```
统一上下文: /smart
路由模式:   /smart/api/flow/{flowCode}
HTTP 方法:  POST（默认）
Content-Type: application/json
```

### 3.3.3 实现方案：统一入口 + 路由分发

采用 **统一 Controller 入口 + 数据库路由表** 的方式，而非运行时动态注册 RequestMapping（避免线程安全和热更新问题）：

```java
@RestController
@RequestMapping("/smart/api/flow")
public class FlowDispatchController {

    @Autowired
    private FlowRouteService flowRouteService;

    @Autowired
    private FlowExecutionService flowExecutionService;

    /**
     * 统一流程调用入口
     * 根据 flowCode 路由到对应的已发布流程
     */
    @PostMapping("/{flowCode}")
    public ResponseEntity<FlowApiResponse> execute(
            @PathVariable String flowCode,
            @RequestBody(required = false) Map<String, Object> body,
            @RequestParam Map<String, String> queryParams,
            HttpServletRequest request) {

        // 1. 查找已发布的流程路由
        FlowRoute route = flowRouteService.getActiveRoute(flowCode);
        if (route == null) {
            return ResponseEntity.status(404)
                .body(FlowApiResponse.fail(404, "流程能力不存在或已下架"));
        }

        // 2. 参数校验
        ValidationResult validation = flowRouteService.validateParams(route, body, queryParams);
        if (!validation.isValid()) {
            return ResponseEntity.badRequest()
                .body(FlowApiResponse.fail(400, validation.getMessage()));
        }

        // 3. 组装 inputs 并执行
        Map<String, Object> inputs = flowRouteService.assembleInputs(route, body, queryParams);
        FlowExecutionResult result = flowExecutionService.executeFlow(route, inputs, request);

        // 4. 返回统一响应
        return ResponseEntity.ok(FlowApiResponse.success(result.getOutputs()));
    }
}
```

### 3.3.4 路由缓存策略

```java
@Service
public class FlowRouteService {

    @Autowired
    private FlowRouteRepository flowRouteRepo;

    @Autowired
    private RedisTemplate<String, FlowRoute> redisTemplate;

    private static final String ROUTE_CACHE_PREFIX = "smart:flow:route:";
    private static final long CACHE_TTL_MINUTES = 30;

    public FlowRoute getActiveRoute(String flowCode) {
        String cacheKey = ROUTE_CACHE_PREFIX + flowCode;
        FlowRoute cached = redisTemplate.opsForValue().get(cacheKey);
        if (cached != null) {
            return cached;
        }
        FlowRoute route = flowRouteRepo.findByFlowCodeAndStatus(flowCode, "ACTIVE");
        if (route != null) {
            redisTemplate.opsForValue().set(cacheKey, route, CACHE_TTL_MINUTES, TimeUnit.MINUTES);
        }
        return route;
    }

    public void invalidateRouteCache(String flowCode) {
        redisTemplate.delete(ROUTE_CACHE_PREFIX + flowCode);
    }
}
```

### 3.3.5 Nacos 注册配置

```yaml
# application.yml
spring:
  application:
    name: smart-flow-engine
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:public}
        group: DEFAULT_GROUP
        metadata:
          version: 1.0.0
          description: 工作流编排调度引擎
      config:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
        file-extension: yaml

server:
  port: 8080
  servlet:
    context-path: /smart

# agents-server 连接配置
flowgram:
  agents-server:
    base-url: http://agents-server:4000
    connect-timeout: 5000
    read-timeout: 60000
    max-connections: 200
  execution:
    poll-interval-ms: 500
    max-wait-seconds: 120
```

### 3.3.6 Feign 客户端（供网关调用参考）

网关侧通过 Nacos 发现 `smart-flow-engine` 服务，使用 Feign 调用：

```java
@FeignClient(name = "smart-flow-engine", path = "/smart")
public interface SmartFlowFeignClient {

    @PostMapping("/api/flow/{flowCode}")
    FlowApiResponse executeFlow(
        @PathVariable("flowCode") String flowCode,
        @RequestBody Map<String, Object> body
    );
}
```

## 3.4 FlowGram Runtime 对接

### 3.4.1 执行服务封装

```java
@Service
public class FlowExecutionService {

    @Autowired
    private RestTemplate restTemplate;

    @Value("${flowgram.agents-server.base-url}")
    private String agentsServerBaseUrl;

    /**
     * 执行流程并等待结果（同步模式）
     */
    public FlowExecutionResult executeFlow(FlowRoute route, Map<String, Object> inputs,
                                            HttpServletRequest request) {
        // 1. 从数据库/缓存加载 schema
        String schemaJson = loadSchema(route.getPublishId());

        // 2. 构建运行请求
        Map<String, Object> runBody = new HashMap<>();
        runBody.put("schema", schemaJson);
        runBody.put("inputs", inputs);

        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        headers.set("x-call-source", "gateway");
        headers.set("x-agent-id", String.valueOf(route.getAgentId()));
        headers.set("x-flow-code", route.getFlowCode());

        // 3. 启动任务
        HttpEntity<Map<String, Object>> entity = new HttpEntity<>(runBody, headers);
        Map<String, Object> runResult = restTemplate.postForObject(
            agentsServerBaseUrl + "/api/task/run", entity, Map.class);

        String taskId = (String) runResult.get("taskID");

        // 4. 轮询等待结果
        return pollForResult(taskId);
    }

    private FlowExecutionResult pollForResult(String taskId) {
        long startTime = System.currentTimeMillis();
        long maxWaitMs = maxWaitSeconds * 1000L;

        while (System.currentTimeMillis() - startTime < maxWaitMs) {
            Map<String, Object> result = restTemplate.getForObject(
                agentsServerBaseUrl + "/api/task/result?taskID=" + taskId, Map.class);

            if (result != null) {
                String status = (String) result.get("status");
                if ("success".equals(status) || "failed".equals(status) || "cancelled".equals(status)) {
                    return FlowExecutionResult.from(result);
                }
            }
            Thread.sleep(pollIntervalMs);
        }
        throw new FlowExecutionTimeoutException("流程执行超时: taskId=" + taskId);
    }
}
```

## 3.5 编辑器与运行时集成

| 运行时模式 | 适用场景 | 说明 |
|-----------|---------|------|
| browser | 仅 demo/纯前端演示 | 前端模拟执行，不落库 |
| server | 编辑器内测试运行 | 直连 agents-server |
| proxy | 生产环境代理 | 通过 Nginx/API 网关代理到 agents-server |

前端编辑器通过 `createRuntimePlugin(mode, serverConfig)` 配置运行时模式。开发环境通过 Rsbuild 代理 `/api/agents`、`/api/task/*` 到 agents-server。

---

# 第四篇 对外调用方案

## 4.1 方案总览

本方案定义从「流程编排完成」到「外部可调用」的完整链路，包含三个核心环节：

```
┌──────────┐      ┌───────────────┐      ┌───────────────┐      ┌──────────────┐
│ 流程编排  │ ──→  │  流程发布      │ ──→  │ 动态路由注册   │ ──→  │  对外可调用   │
│ (编辑器)  │      │ (参数提取+API) │      │ (Nacos+路由表) │      │ (双网关入口)  │
└──────────┘      └───────────────┘      └───────────────┘      └──────────────┘
```

## 4.2 流程发布

### 4.2.1 发布流程

```
编辑器「发布」按钮
    │
    ▼
┌──────────────────────────────────────────────┐
│  Step 1: 参数自动提取                         │
│  - 解析 design_json 中 start 节点的 inputs    │
│  - 自动识别字段名、推断类型、提取默认值         │
│  - 标记必填字段（有值字段默认必填）             │
└──────────────────────┬───────────────────────┘
                       ▼
┌──────────────────────────────────────────────┐
│  Step 2: 发布信息填写                         │
│  - 能力名称、描述、分类标签                    │
│  - flowCode（路由标识，自动生成可修改）         │
│  - 版本号（默认 1.0.0）                       │
│  - 鉴权方式选择                               │
│  - 参数元数据编辑（可调整类型、必填、描述）      │
│  - 响应结构体示例 + 说明                       │
└──────────────────────┬───────────────────────┘
                       ▼
┌──────────────────────────────────────────────┐
│  Step 3: 发布校验                             │
│  - 必填项检查                                 │
│  - flowCode 唯一性校验                        │
│  - Schema 合法性校验（调用 validate 接口）      │
│  - 试运行检查（可选）                          │
└──────────────────────┬───────────────────────┘
                       ▼
┌──────────────────────────────────────────────┐
│  Step 4: 发布执行                             │
│  - 写入 flow_publish 表（发布记录）            │
│  - 写入 flow_api_params 表（参数定义）         │
│  - 写入 flow_route 表（路由注册）              │
│  - Schema JSON 加密存储（对外不可见）           │
│  - 清除路由缓存，使新路由立即生效               │
│  - 返回发布结果（含调用地址）                   │
└──────────────────────────────────────────────┘
```

### 4.2.2 参数自动提取算法

从 `design_json` 的 start 节点中提取 inputs，自动推断类型：

```java
public class InputParamExtractor {

    /**
     * 从 design_json 提取 start 节点的 inputs 参数定义
     * 
     * 输入示例:
     * inputs: {
     *   alarmTimeStart: "2021-3-2 23:59:59",
     *   pageNum: 1,
     *   alarmTimeEnd: "2026-3-10 23:59:59",
     *   pageSize: 6
     * }
     * 
     * 输出: 参数列表，含 name / type / required / defaultValue / description
     */
    public List<FlowParamDefinition> extractInputParams(String designJson) {
        JsonNode root = objectMapper.readTree(designJson);
        JsonNode startNode = findStartNode(root);
        JsonNode inputs = extractInputsFromStartNode(startNode);

        List<FlowParamDefinition> params = new ArrayList<>();
        Iterator<Map.Entry<String, JsonNode>> fields = inputs.fields();

        while (fields.hasNext()) {
            Map.Entry<String, JsonNode> field = fields.next();
            FlowParamDefinition param = new FlowParamDefinition();
            param.setParamName(field.getKey());
            param.setParamType(inferType(field.getValue()));
            param.setRequired(true);  // 默认必填
            param.setDefaultValue(field.getValue().isNull() ? null : field.getValue().asText());
            param.setDescription("");  // 待用户补充
            params.add(param);
        }
        return params;
    }

    private String inferType(JsonNode value) {
        if (value.isTextual()) return "String";
        if (value.isInt()) return "Integer";
        if (value.isLong()) return "Long";
        if (value.isDouble() || value.isFloat()) return "Double";
        if (value.isBoolean()) return "Boolean";
        if (value.isArray()) return "Array";
        if (value.isObject()) return "Object";
        return "String";  // 默认 String
    }
}
```

以用户示例为例，自动提取结果：

| 参数名 | 推断类型 | 必填 | 默认值 | 描述 |
|--------|---------|------|--------|------|
| alarmTimeStart | String | 是 | 2021-3-2 23:59:59 | （待填写） |
| pageNum | Integer | 是 | 1 | （待填写） |
| alarmTimeEnd | String | 是 | 2026-3-10 23:59:59 | （待填写） |
| pageSize | Integer | 是 | 6 | （待填写） |

### 4.2.3 发布 API 接口定义

```
POST /smart/api/publish
```

**请求体**：

```json
{
  "agentId": 5,
  "flowCode": "warning",
  "name": "告警查询能力",
  "description": "根据时间范围和分页参数查询告警信息",
  "version": "1.0.0",
  "category": "告警管理",
  "tags": ["告警", "查询"],
  "authModes": ["SCENE_CODE", "ACCESS_KEY"],
  "params": [
    {
      "paramName": "alarmTimeStart",
      "paramType": "String",
      "paramIn": "body",
      "required": true,
      "defaultValue": "2021-3-2 23:59:59",
      "description": "告警开始时间，格式 yyyy-M-d HH:mm:ss"
    },
    {
      "paramName": "pageNum",
      "paramType": "Integer",
      "paramIn": "body",
      "required": true,
      "defaultValue": "1",
      "description": "页码，从1开始"
    },
    {
      "paramName": "alarmTimeEnd",
      "paramType": "String",
      "paramIn": "body",
      "required": true,
      "defaultValue": "2026-3-10 23:59:59",
      "description": "告警结束时间"
    },
    {
      "paramName": "pageSize",
      "paramType": "Integer",
      "paramIn": "body",
      "required": true,
      "defaultValue": "6",
      "description": "每页条数"
    }
  ],
  "responseExample": {
    "code": 200,
    "msg": "请求成功",
    "data": {
      "total": 100,
      "list": [
        {
          "processId": "f6dd80b0487b4f...",
          "cityName": "林芝市",
          "latitude": "29.637732",
          "orderStatus": "1"
        }
      ]
    }
  },
  "responseDescription": [
    { "field": "code", "type": "Integer", "description": "状态码，200表示成功" },
    { "field": "msg", "type": "String", "description": "响应消息" },
    { "field": "data.total", "type": "Integer", "description": "总记录数" },
    { "field": "data.list", "type": "Array", "description": "数据列表" },
    { "field": "data.list[].processId", "type": "String", "description": "流程ID" },
    { "field": "data.list[].cityName", "type": "String", "description": "城市名称" }
  ]
}
```

**响应体**：

```json
{
  "code": 200,
  "msg": "发布成功",
  "data": {
    "publishId": 1001,
    "flowCode": "warning",
    "routePath": "/smart/api/flow/warning",
    "fullUrl": "https://gateway.example.com/smart/api/flow/warning",
    "status": "ACTIVE",
    "version": "1.0.0",
    "publishTime": "2026-03-03T14:30:00+08:00"
  }
}
```

## 4.3 动态路由注册

### 4.3.1 路由注册流程

```
发布服务
    │
    ├─ 1. 写入 flow_route 表（flowCode, publishId, agentId, status=ACTIVE）
    │
    ├─ 2. 写入 flow_publish 表（含 schema_json 加密存储）
    │
    ├─ 3. 写入 flow_api_params 表（参数定义）
    │
    ├─ 4. 清除 Redis 路由缓存
    │     └─ DEL smart:flow:route:{flowCode}
    │
    └─ 5. 路由立即生效
          └─ 下次请求 /smart/api/flow/{flowCode} 时
             FlowDispatchController 查到 ACTIVE 路由即可调度
```

### 4.3.2 路由规则与约束

| 规则 | 说明 |
|------|------|
| **flowCode 唯一性** | 同一时间只能有一个 ACTIVE 状态的同名 flowCode |
| **flowCode 命名规范** | 仅允许小写字母、数字、短横线，长度 3~64，如 `warning`、`device-query`、`alarm-list` |
| **路由路径格式** | 固定前缀 `/smart/api/flow/` + flowCode |
| **HTTP 方法** | 默认 POST，统一使用 JSON Body 传参 |
| **版本化路由** | 支持 `/smart/api/flow/v2/warning` 形式的版本路由（可选） |
| **路由状态流转** | DRAFT → ACTIVE → INACTIVE（下架） → ACTIVE（重新上架） |
| **并发安全** | 发布/下架操作需加分布式锁（Redis SETNX），防止并发冲突 |
| **优雅下线** | 下架时等待进行中的请求完成（最长等待 30s），再标记 INACTIVE |

### 4.3.3 路由状态机

```
          publish()             unpublish()           republish()
 DRAFT ───────────→ ACTIVE ──────────────→ INACTIVE ──────────────→ ACTIVE
   │                  │                       │
   │   delete()       │   update()            │   delete()
   ▼                  ▼                       ▼
 DELETED         新版本 ACTIVE              DELETED
                 旧版本 INACTIVE
```

## 4.4 API 接口定义规范

### 4.4.1 统一调用地址

| 入口 | 调用地址 | 鉴权方式 |
|------|---------|---------|
| **视联平台网关** | `https://slpt-gateway.example.com/smart/api/flow/{flowCode}` | SceneCode 简单身份认证 |
| **能力开放网关** | `https://open-gateway.example.com/smart/api/flow/{flowCode}` | AccessKey 签名认证 |
| **内网直连**（开发/测试） | `http://smart-flow-engine:8080/smart/api/flow/{flowCode}` | 无鉴权 / Basic Auth |

### 4.4.2 通用请求规范

#### 请求头（Headers）

| Header | 必填 | 说明 |
|--------|------|------|
| Content-Type | 是 | 固定 `application/json` |
| X-Scene-Code | 视联网关必填 | 视联平台分配的场景码 |
| X-Access-Key | 开放网关必填 | 能力开放平台分配的 AccessKey |
| X-Signature | 开放网关必填 | 请求签名（HMAC-SHA256） |
| X-Timestamp | 开放网关必填 | 请求时间戳（毫秒），有效期 ±5 分钟 |
| X-Nonce | 开放网关建议 | 随机字符串，防重放 |
| X-Request-Id | 否 | 调用方自定义请求追踪 ID |

#### 请求参数（Query）

通常不使用 Query 参数，所有业务参数统一放 Body。但保留扩展能力：

| Query 参数 | 必填 | 说明 |
|-----------|------|------|
| _version | 否 | 指定调用的流程版本号，默认调用最新 ACTIVE 版本 |
| _async | 否 | `true` 时异步执行，立即返回 taskId，调用方自行轮询结果 |
| _timeout | 否 | 自定义超时时间（秒），覆盖默认值 |

#### 请求体（Body）

Body 结构为扁平 JSON 对象，字段与发布时定义的参数一一对应：

```json
{
  "alarmTimeStart": "2021-3-2 23:59:59",
  "pageNum": 1,
  "alarmTimeEnd": "2026-3-10 23:59:59",
  "pageSize": 6
}
```

### 4.4.3 统一响应规范

#### 同步模式响应

```json
{
  "code": 200,
  "msg": "请求成功",
  "requestId": "req-20260303-abcdef",
  "timestamp": 1741012345678,
  "data": {
    // 工作流 end 节点的 outputs
  }
}
```

#### 异步模式响应（`_async=true`）

```json
{
  "code": 200,
  "msg": "任务已提交",
  "requestId": "req-20260303-abcdef",
  "timestamp": 1741012345678,
  "data": {
    "taskId": "task-xxxx-xxxx",
    "statusUrl": "/smart/api/flow/warning/status?taskId=task-xxxx-xxxx",
    "resultUrl": "/smart/api/flow/warning/result?taskId=task-xxxx-xxxx"
  }
}
```

#### 异步结果查询

```
GET /smart/api/flow/{flowCode}/result?taskId={taskId}
```

```json
{
  "code": 200,
  "msg": "执行完成",
  "data": {
    "taskId": "task-xxxx-xxxx",
    "status": "success",
    "outputs": { ... },
    "startTime": "2026-03-03T14:30:00+08:00",
    "endTime": "2026-03-03T14:30:02+08:00",
    "durationMs": 2150
  }
}
```

#### 错误码定义

| code | msg | 说明 |
|------|-----|------|
| 200 | 请求成功 | 正常返回 |
| 400 | 参数校验失败 | 必填参数缺失或类型错误 |
| 401 | 认证失败 | SceneCode/AccessKey 无效或签名错误 |
| 403 | 无权限 | 未订阅该能力或配额已用尽 |
| 404 | 能力不存在 | flowCode 不存在或已下架 |
| 408 | 执行超时 | 工作流执行超过超时时间 |
| 429 | 请求频率超限 | 超出调用频率限制 |
| 500 | 系统内部错误 | 服务端异常 |
| 502 | 上游服务异常 | agents-server 不可用 |

### 4.4.4 鉴权方案详解

#### 方案一：SceneCode 简单身份认证（视联平台网关）

适用于视联平台内部系统调用，鉴权流程：

```
调用方                          视联平台网关                    smart-flow-engine
  │                                │                              │
  │  POST /smart/api/flow/warning  │                              │
  │  Header: X-Scene-Code: SC001   │                              │
  │ ──────────────────────────────→│                              │
  │                                │  校验 SceneCode              │
  │                                │  ├─ 是否存在                 │
  │                                │  ├─ 是否有该流程调用权限      │
  │                                │  └─ 是否在有效期内           │
  │                                │                              │
  │                                │  Feign 调用 (Nacos发现)      │
  │                                │ ────────────────────────────→│
  │                                │                              │  执行流程
  │                                │         响应                  │
  │                                │ ←────────────────────────────│
  │         响应                    │                              │
  │ ←──────────────────────────────│                              │
```

#### 方案二：AccessKey 签名认证（能力开放网关）

适用于第三方系统调用，安全级别更高：

**签名算法**：

```
1. 构造待签名字符串:
   StringToSign = HTTP-Method + "\n"
                + URI + "\n"
                + Timestamp + "\n"
                + Nonce + "\n"
                + SHA256(RequestBody)

2. 计算签名:
   Signature = Base64(HMAC-SHA256(AccessKeySecret, StringToSign))

3. 请求头:
   X-Access-Key: AK20260303xxxxx
   X-Signature: <Signature>
   X-Timestamp: 1741012345678
   X-Nonce: random-string-uuid
```

**调用示例（Java）**：

```java
public class OpenGatewayClient {

    private String accessKey = "AK20260303xxxxx";
    private String accessSecret = "your-secret-key";
    private String gatewayUrl = "https://open-gateway.example.com";

    public String callFlow(String flowCode, String bodyJson) throws Exception {
        String uri = "/smart/api/flow/" + flowCode;
        String timestamp = String.valueOf(System.currentTimeMillis());
        String nonce = UUID.randomUUID().toString();
        String bodyHash = sha256Hex(bodyJson);

        String stringToSign = "POST\n" + uri + "\n" + timestamp + "\n" + nonce + "\n" + bodyHash;
        String signature = base64(hmacSha256(accessSecret, stringToSign));

        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create(gatewayUrl + uri))
            .header("Content-Type", "application/json")
            .header("X-Access-Key", accessKey)
            .header("X-Signature", signature)
            .header("X-Timestamp", timestamp)
            .header("X-Nonce", nonce)
            .POST(HttpRequest.BodyPublishers.ofString(bodyJson))
            .build();

        return httpClient.send(request, HttpResponse.BodyHandlers.ofString()).body();
    }
}
```

**调用示例（cURL）**：

```bash
curl -X POST "https://open-gateway.example.com/smart/api/flow/warning" \
  -H "Content-Type: application/json" \
  -H "X-Access-Key: AK20260303xxxxx" \
  -H "X-Signature: <computed-signature>" \
  -H "X-Timestamp: 1741012345678" \
  -H "X-Nonce: random-uuid" \
  -d '{
    "alarmTimeStart": "2021-3-2 23:59:59",
    "pageNum": 1,
    "alarmTimeEnd": "2026-3-10 23:59:59",
    "pageSize": 6
  }'
```

### 4.4.5 调用方使用流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         调用方使用全流程                                     │
│                                                                             │
│  1. 发现能力                                                                │
│     └─ 进入「能力市场」页面 /marketplace                                     │
│     └─ 按分类/标签/关键词搜索所需的流程能力                                   │
│                                                                             │
│  2. 查看文档                                                                │
│     └─ 点击能力卡片进入详情页                                                │
│     └─ 查看 API 地址、请求参数、响应结构、鉴权方式、使用示例                    │
│                                                                             │
│  3. 获取凭证                                                                │
│     └─ 点击「申请调用」/ 「订阅」                                            │
│     └─ 视联平台：获取 SceneCode                                             │
│     └─ 开放网关：获取 AccessKey + AccessKeySecret                           │
│                                                                             │
│  4. 在线调试                                                                │
│     └─ 点击「去调试」按钮                                                    │
│     └─ 在调试工具中填写参数、自动携带凭证、发起请求                            │
│     └─ 查看请求/响应结果                                                     │
│                                                                             │
│  5. 正式对接                                                                │
│     └─ 业务后台按 API 文档构造 HTTP 请求                                     │
│     └─ 通过网关调用 POST /smart/api/flow/{flowCode}                         │
│     └─ 解析统一响应结构获取业务数据                                           │
│                                                                             │
│  6. 运行监控                                                                │
│     └─ 在能力市场「我的订阅」查看调用统计                                     │
│     └─ 查看调用量、成功率、平均耗时                                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 4.5 能力详情页 API 文档展示规范

发布后自动生成的 API 文档页面应包含以下信息：

### 4.5.1 API 接口区

```
┌─────────────────────────────────────────────────────────┐
│  API 接口                                               │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  告警查询能力 v1.0.0                               │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  调用地址:  /smart/api/flow/warning                      │
│  请求方式:  POST                                        │
│  返回类型:  JSON                                        │
│                                                         │
│  API调用:  API简单身份认证调用方法（SceneCode） 收起 ▲    │
│            API签名认证调用方法（AccessKey）               │
│                                                         │
│  调试工具:  [去调试]                                     │
│                                                         │
│  ┌─ 请求参数（Headers）────────────────────────────┐     │
│  │ 参数名          类型      必填   说明            │     │
│  │ Content-Type   String    是    application/json │     │
│  │ X-Scene-Code   String    是*   场景码(视联网关)  │     │
│  │ X-Access-Key   String    是*   密钥(开放网关)    │     │
│  └──────────────────────────────────────────────────┘    │
│                                                         │
│  ┌─ 请求参数（Body）───────────────────────────────┐     │
│  │ 参数名           类型      必填  默认值          │     │
│  │                                  说明            │     │
│  │ alarmTimeStart  String    是    2021-3-2 23...   │     │
│  │                                  告警开始时间     │     │
│  │ pageNum         Integer   是    1                │     │
│  │                                  页码            │     │
│  │ alarmTimeEnd    String    是    2026-3-10 23...  │     │
│  │                                  告警结束时间     │     │
│  │ pageSize        Integer   是    6                │     │
│  │                                  每页条数         │     │
│  └──────────────────────────────────────────────────┘    │
│                                                         │
│  ┌─ 返回内容 ──────────────────────────────────────┐     │
│  │ 字段              类型     说明                  │     │
│  │ code             Integer  状态码                 │     │
│  │ msg              String   响应消息               │     │
│  │ data.total       Integer  总记录数               │     │
│  │ data.list        Array    数据列表               │     │
│  │ data.list[].processId  String  流程ID            │     │
│  │ data.list[].cityName   String  城市名称          │     │
│  └──────────────────────────────────────────────────┘    │
│                                                         │
│  ▼ 使用示例                                              │
└─────────────────────────────────────────────────────────┘
```

### 4.5.2 使用示例区

```json
// === 请求示例 ===
POST /smart/api/flow/warning
Content-Type: application/json
X-Scene-Code: SC-WARNING-001

{
  "alarmTimeStart": "2021-3-2 23:59:59",
  "pageNum": 1,
  "alarmTimeEnd": "2026-3-10 23:59:59",
  "pageSize": 6
}

// === 响应示例 ===
{
  "code": 200,
  "msg": "请求成功",
  "requestId": "req-20260303-f8a3b2c1",
  "timestamp": 1741012345678,
  "data": {
    "total": 156,
    "list": [
      {
        "processId": "f6dd80b0487b4f...",
        "relLinkType": "1",
        "flowEndTime": "...",
        "relDevice": {},
        "videoList": [],
        "cityName": "林芝市",
        "latitude": "29.637732",
        "orderStatus": "1",
        "warningLevel": "高"
      }
    ]
  }
}
```

---

# 第五篇 数据库设计

## 5.1 核心表结构

### 5.1.1 智能体表 (agents)

```sql
CREATE TABLE agents (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    name            VARCHAR(200)  NOT NULL COMMENT '智能体名称',
    description     VARCHAR(1000) DEFAULT NULL COMMENT '描述',
    design_json     LONGTEXT      DEFAULT NULL COMMENT '工作流设计 JSON（FlowGram Schema）',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='智能体表';
```

### 5.1.2 任务执行记录表 (task_executions)

```sql
CREATE TABLE task_executions (
    id                  BIGINT AUTO_INCREMENT PRIMARY KEY,
    task_id             VARCHAR(64)   NOT NULL COMMENT '任务ID（FlowGram 生成）',
    agent_id            BIGINT        DEFAULT NULL COMMENT '关联智能体ID',
    flow_code           VARCHAR(64)   DEFAULT NULL COMMENT '关联发布流程编码',
    source              VARCHAR(20)   NOT NULL DEFAULT 'api' COMMENT '来源: page/api/gateway',
    status              VARCHAR(20)   NOT NULL DEFAULT 'running' COMMENT '状态: running/success/failed/cancelled',
    input_params        LONGTEXT      DEFAULT NULL COMMENT '输入参数 JSON',
    output_result       LONGTEXT      DEFAULT NULL COMMENT '输出结果 JSON',
    error_message       TEXT          DEFAULT NULL COMMENT '错误信息',
    execution_log       LONGTEXT      DEFAULT NULL COMMENT '执行日志 JSON',
    node_logs           LONGTEXT      DEFAULT NULL COMMENT '节点日志 JSON',
    start_time          DATETIME      DEFAULT NULL,
    end_time            DATETIME      DEFAULT NULL,
    total_duration_ms   BIGINT        DEFAULT NULL COMMENT '总耗时(毫秒)',
    caller_ip           VARCHAR(45)   DEFAULT NULL COMMENT '调用方IP',
    request_id          VARCHAR(64)   DEFAULT NULL COMMENT '请求追踪ID',
    created_at          DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_task_id (task_id),
    INDEX idx_agent_id (agent_id),
    INDEX idx_flow_code (flow_code),
    INDEX idx_status (status),
    INDEX idx_start_time (start_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务执行记录表';
```

### 5.1.3 流程发布表 (flow_publish)

```sql
CREATE TABLE flow_publish (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    agent_id        BIGINT        NOT NULL COMMENT '关联智能体ID',
    flow_code       VARCHAR(64)   NOT NULL COMMENT '流程编码（路由标识）',
    name            VARCHAR(200)  NOT NULL COMMENT '能力名称',
    description     VARCHAR(2000) DEFAULT NULL COMMENT '能力描述',
    version         VARCHAR(20)   NOT NULL DEFAULT '1.0.0' COMMENT '版本号',
    category        VARCHAR(100)  DEFAULT NULL COMMENT '分类',
    tags            VARCHAR(500)  DEFAULT NULL COMMENT '标签，逗号分隔',
    schema_json     LONGTEXT      NOT NULL COMMENT '工作流 Schema JSON（加密存储，对外不可见）',
    auth_modes      VARCHAR(100)  NOT NULL DEFAULT 'SCENE_CODE' COMMENT '支持的鉴权方式，逗号分隔',
    status          VARCHAR(20)   NOT NULL DEFAULT 'ACTIVE' COMMENT '状态: DRAFT/ACTIVE/INACTIVE/DELETED',
    response_example     LONGTEXT DEFAULT NULL COMMENT '响应示例 JSON',
    response_description LONGTEXT DEFAULT NULL COMMENT '响应字段说明 JSON',
    published_by    VARCHAR(100)  DEFAULT NULL COMMENT '发布人',
    published_at    DATETIME      DEFAULT NULL COMMENT '发布时间',
    unpublished_at  DATETIME      DEFAULT NULL COMMENT '下架时间',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_flow_code_version (flow_code, version),
    INDEX idx_agent_id (agent_id),
    INDEX idx_status (status),
    INDEX idx_category (category)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='流程发布表';
```

### 5.1.4 流程 API 参数表 (flow_api_params)

```sql
CREATE TABLE flow_api_params (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    publish_id      BIGINT        NOT NULL COMMENT '关联发布ID',
    param_name      VARCHAR(100)  NOT NULL COMMENT '参数名',
    param_type      VARCHAR(20)   NOT NULL DEFAULT 'String' COMMENT '参数类型: String/Integer/Long/Double/Boolean/Object/Array',
    param_in        VARCHAR(20)   NOT NULL DEFAULT 'body' COMMENT '参数位置: header/query/body',
    required        TINYINT(1)    NOT NULL DEFAULT 1 COMMENT '是否必填: 0-否 1-是',
    default_value   VARCHAR(500)  DEFAULT NULL COMMENT '默认值',
    description     VARCHAR(1000) DEFAULT NULL COMMENT '参数说明',
    example_value   VARCHAR(500)  DEFAULT NULL COMMENT '示例值',
    sort_order      INT           NOT NULL DEFAULT 0 COMMENT '排序序号',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_publish_id (publish_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='流程API参数定义表';
```

### 5.1.5 流程路由表 (flow_route)

```sql
CREATE TABLE flow_route (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    flow_code       VARCHAR(64)   NOT NULL COMMENT '流程编码',
    publish_id      BIGINT        NOT NULL COMMENT '关联发布记录ID',
    agent_id        BIGINT        NOT NULL COMMENT '关联智能体ID',
    route_path      VARCHAR(200)  NOT NULL COMMENT '完整路由路径',
    http_method     VARCHAR(10)   NOT NULL DEFAULT 'POST' COMMENT 'HTTP方法',
    status          VARCHAR(20)   NOT NULL DEFAULT 'ACTIVE' COMMENT '路由状态: ACTIVE/INACTIVE',
    timeout_seconds INT           NOT NULL DEFAULT 120 COMMENT '超时时间(秒)',
    rate_limit_qps  INT           DEFAULT NULL COMMENT '限流 QPS',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_flow_code_active (flow_code, status),
    INDEX idx_publish_id (publish_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='流程路由表';
```

### 5.1.6 能力订阅表 (flow_subscription)

```sql
CREATE TABLE flow_subscription (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    flow_code       VARCHAR(64)   NOT NULL COMMENT '流程编码',
    subscriber_id   VARCHAR(100)  NOT NULL COMMENT '订阅方标识',
    subscriber_name VARCHAR(200)  DEFAULT NULL COMMENT '订阅方名称',
    auth_mode       VARCHAR(20)   NOT NULL COMMENT '鉴权方式: SCENE_CODE/ACCESS_KEY',
    credential      VARCHAR(200)  NOT NULL COMMENT '凭证（SceneCode 或 AccessKey）',
    quota_daily     INT           DEFAULT NULL COMMENT '每日配额(-1 无限)',
    status          VARCHAR(20)   NOT NULL DEFAULT 'ACTIVE' COMMENT '状态: ACTIVE/SUSPENDED/REVOKED',
    expires_at      DATETIME      DEFAULT NULL COMMENT '过期时间（NULL不过期）',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_credential (credential),
    INDEX idx_flow_subscriber (flow_code, subscriber_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='能力订阅表';
```

### 5.1.7 调用统计表 (flow_call_stats)

```sql
CREATE TABLE flow_call_stats (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    flow_code       VARCHAR(64)   NOT NULL COMMENT '流程编码',
    stat_date       DATE          NOT NULL COMMENT '统计日期',
    total_calls     BIGINT        NOT NULL DEFAULT 0 COMMENT '总调用次数',
    success_calls   BIGINT        NOT NULL DEFAULT 0 COMMENT '成功次数',
    failed_calls    BIGINT        NOT NULL DEFAULT 0 COMMENT '失败次数',
    avg_duration_ms BIGINT        DEFAULT NULL COMMENT '平均耗时(毫秒)',
    max_duration_ms BIGINT        DEFAULT NULL COMMENT '最大耗时(毫秒)',
    p99_duration_ms BIGINT        DEFAULT NULL COMMENT 'P99耗时(毫秒)',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_flow_date (flow_code, stat_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='调用统计表（按日聚合）';
```

## 5.2 ER 关系

```
agents (1) ──────── (N) flow_publish
                          │
                          ├── (N) flow_api_params
                          │
                          └── (1) flow_route
                                    │
                                    └── (N) flow_subscription
                                    └── (N) flow_call_stats

agents (1) ──────── (N) task_executions
flow_route (1) ──── (N) task_executions (通过 flow_code 关联)
```

---

# 第六篇 部署与运维

## 6.1 部署架构

```
┌─────────────────── K8s / Docker Compose ───────────────────┐
│                                                             │
│  ┌─────────────────┐   ┌─────────────────────────────────┐ │
│  │  前端 (Nginx)    │   │  smart-flow-engine (Java)       │ │
│  │  :80 / :443      │   │  :8080                          │ │
│  │  /agent → 静态   │   │  注册到 Nacos                    │ │
│  │  /api → 代理后端  │   │  Instances: 2+ (HA)            │ │
│  └────────┬────────┘   └────────────────┬────────────────┘ │
│           │                              │                  │
│  ┌────────▼────────┐   ┌────────────────▼────────────────┐ │
│  │  agents-server   │   │  MySQL 8.0                      │ │
│  │  (Node.js)       │   │  主从 / RDS                     │ │
│  │  :4000           │   └─────────────────────────────────┘ │
│  │  Instances: 2+   │                                       │
│  └─────────────────┘   ┌─────────────────────────────────┐ │
│                         │  Redis 6.x (Sentinel/Cluster)   │ │
│  ┌─────────────────┐   │  路由缓存 / Schema缓存 / 限流    │ │
│  │  Nacos 2.x       │   └─────────────────────────────────┘ │
│  │  集群模式 (3节点) │                                       │
│  └─────────────────┘                                        │
└─────────────────────────────────────────────────────────────┘
```

## 6.2 关键运维指标

| 指标 | 阈值 | 处理方式 |
|------|------|---------|
| API 响应时间 P99 | > 5s | 告警 → 检查 agents-server 负载 |
| 调用失败率 | > 5% | 告警 → 检查上游服务可用性 |
| agents-server 内存 | > 80% | 触发 cleanup → 扩容 |
| MySQL 慢查询 | > 1s | 优化索引或 SQL |
| Redis 命中率 | < 90% | 检查缓存策略 |

## 6.3 健康检查

| 服务 | 端点 | 说明 |
|------|------|------|
| smart-flow-engine | `GET /smart/actuator/health` | Spring Boot Actuator |
| agents-server | `GET /health` | Express 健康检查 |
| 前端 | `GET /agent/` | Nginx 静态资源 |

## 6.4 日志规范

| 层次 | 日志内容 | 存储 |
|------|---------|------|
| 网关层 | 请求 URL、鉴权结果、调用方标识、耗时 | 网关日志系统 |
| 路由层 | flowCode、参数校验结果、路由匹配 | smart-flow-engine 日志 |
| 执行层 | taskId、节点执行状态、输入输出 | task_executions 表 + 应用日志 |

---

# 附录

## 附录 A：完整调用时序图

```
调用方           视联平台网关         smart-flow-engine        agents-server       MySQL/Redis
  │                  │                      │                      │                  │
  │  POST /smart/    │                      │                      │                  │
  │  api/flow/       │                      │                      │                  │
  │  warning         │                      │                      │                  │
  │  + SceneCode     │                      │                      │                  │
  │ ────────────────→│                      │                      │                  │
  │                  │                      │                      │                  │
  │                  │ 校验 SceneCode       │                      │                  │
  │                  │ Nacos 发现服务        │                      │                  │
  │                  │                      │                      │                  │
  │                  │  Feign POST          │                      │                  │
  │                  │  /smart/api/flow/    │                      │                  │
  │                  │  warning             │                      │                  │
  │                  │ ───────────────────→ │                      │                  │
  │                  │                      │                      │                  │
  │                  │                      │ 1. 查路由(Redis缓存)  │                  │
  │                  │                      │ ─────────────────────────────────────→  │
  │                  │                      │ ←─────────────────────────────────────  │
  │                  │                      │                      │                  │
  │                  │                      │ 2. 加载Schema        │                  │
  │                  │                      │   (Redis→MySQL)      │                  │
  │                  │                      │ ─────────────────────────────────────→  │
  │                  │                      │ ←─────────────────────────────────────  │
  │                  │                      │                      │                  │
  │                  │                      │ 3. 参数校验 & 组装    │                  │
  │                  │                      │                      │                  │
  │                  │                      │ 4. POST /api/task/run│                  │
  │                  │                      │ ───────────────────→ │                  │
  │                  │                      │                      │ 执行 FlowGram    │
  │                  │                      │                      │ runtime-js       │
  │                  │                      │                      │                  │
  │                  │                      │ 5. 轮询 result       │                  │
  │                  │                      │ ···················→ │                  │
  │                  │                      │ ←··················· │                  │
  │                  │                      │                      │                  │
  │                  │                      │ 6. 记录执行日志       │                  │
  │                  │                      │ ─────────────────────────────────────→  │
  │                  │                      │                      │                  │
  │                  │   统一响应            │                      │                  │
  │                  │ ←─────────────────── │                      │                  │
  │                  │                      │                      │                  │
  │   响应           │                      │                      │                  │
  │ ←────────────────│                      │                      │                  │
```

## 附录 B：flowCode 编码规范

| 规则 | 说明 | 示例 |
|------|------|------|
| 字符集 | 小写字母 + 数字 + 短横线 | `warning`、`device-query` |
| 长度 | 3 ~ 64 字符 | |
| 不允许 | 大写、下划线、中文、特殊字符 | |
| 建议命名 | `{业务域}-{动作}` 或 `{业务域}-{对象}-{动作}` | `alarm-list`、`device-bindstatus-query` |

## 附录 C：参数类型映射

| 推断类型 | Java 类型 | JSON Schema type | 说明 |
|---------|----------|-----------------|------|
| String | String | string | 默认类型 |
| Integer | Integer | integer | 32 位整数 |
| Long | Long | integer | 64 位整数 |
| Double | Double | number | 浮点数 |
| Boolean | Boolean | boolean | 布尔值 |
| Object | Map<String, Object> | object | 嵌套对象 |
| Array | List<Object> | array | 数组 |

## 附录 D：错误排查指引

| 现象 | 可能原因 | 排查步骤 |
|------|---------|---------|
| 404 能力不存在 | flowCode 错误或已下架 | 检查 flow_route 表 status 字段 |
| 400 参数校验失败 | 必填参数缺失或类型不匹配 | 查看响应 msg 中的具体字段 |
| 408 执行超时 | 工作流内节点耗时过长 | 查看 task_executions 中 node_logs |
| 502 上游服务异常 | agents-server 不可用 | 检查 agents-server 健康状态 |
| 401 认证失败 | 凭证无效或签名错误 | 检查 SceneCode/AccessKey 有效性、签名算法 |

## 附录 E：功能与底层技术映射

| 功能 | FlowGram.AI 能力 |
|------|------------------|
| 自由布局画布、节点、连线 | @flowgram.ai/free-layout-editor + 插件体系 |
| 节点表单、侧边栏 | form-materials、panel-manager-plugin |
| 变量 | variableEngine、VariablePanelPlugin |
| 运行与轮询 | runtime-js 的 TaskRunAPI/ReportAPI/ResultAPI/CancelAPI/ValidateAPI |
| 执行记录 | 自研 agents-server + MySQL |
| **流程发布** | **自研 smart-flow-engine (Spring Boot)** |
| **动态路由** | **统一 Controller + 路由表 + Redis 缓存** |
| **双网关接入** | **Nacos 注册 + Feign 调用** |

## 附录 F：文件/接口索引

| 功能域 | 前端入口 | 后端接口 |
|--------|---------|---------|
| 智能体管理 | `/manage` | `GET/POST/PUT/DELETE /api/agents` |
| 工作流编辑 | `/editor/:id` | `GET/PUT /api/agents/:id` |
| 任务执行 | 编辑器运行按钮 | `POST /api/task/run`、`GET /api/task/report`、`GET /api/task/result` |
| **流程发布** | 编辑器「发布」 | `POST /smart/api/publish`、`PUT /smart/api/publish/:id/unpublish` |
| **动态路由调用** | 能力市场调试 | `POST /smart/api/flow/{flowCode}` |
| **能力市场** | `/marketplace` | `GET /smart/api/marketplace/list`、`GET /smart/api/marketplace/:flowCode` |
| 运行日志 | 管理页弹窗 | `GET /api/agents/:id/executions` |

---

> **文档说明**: 本文档整合了工作流编排工具的功能架构、技术架构与对外调用方案，形成调度服务引擎的总体建设方案。随项目演进可持续迭代更新。
