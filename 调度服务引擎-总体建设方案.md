<div align="center">

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>

# 能力引擎平台 — 总体建设方案

<br/><br/><br/><br/><br/><br/><br/><br/><br/><br/>

**浩鲸科技股份有限公司**

</div>

<div style="page-break-after: always;"></div>

---

## 修改记录

| 文件编号 | 版本号 | 拟制人/修改人 | 拟制/修改日期 | 更改理由 | 主要更改内容（写要点即可） |
|---------|--------|------------|-------------|---------|------------------------|
| SMART-FLOW-001 | V1.0.0 | 胡庆云 | 2026-03-03 | 初次编写 | 无 |
| | | | | | |
| | | | | | |
| | | | | | |

> 注 1：每次更改归档文件，需填写此表  

<div style="page-break-after: always;"></div>

---

## 目  录

- [1 方案概述](#1-方案概述)
  - [1.1 项目背景](#11-项目背景)
  - [1.2 系统定位](#12-系统定位)
  - [1.3 总体架构概览](#13-总体架构概览)
- [2 功能架构](#2-功能架构)
  - [2.1 业务闭环与模块总览](#21-业务闭环与模块总览)
  - [2.2 用户角色](#22-用户角色)
  - [2.3 模块一：能力接入](#23-模块一能力接入管理前端)
    - [2.3.1 能力接入上架](#231-能力接入上架)
    - [2.3.2 能力文档管理](#232-能力文档管理)
    - [2.3.3 能力版本管理](#233-能力版本管理)
  - [2.4 模块二：能力编排](#24-模块二能力编排管理前端)
    - [2.4.1 能力目录（功能域）](#241-能力目录功能域)
    - [2.4.2 能力编排模块](#242-能力编排模块)
    - [2.4.3 运行日志](#243-运行日志)
  - [2.5 模块三：能力发布](#25-模块三能力发布管理前端)
    - [2.5.1 发布列表](#251-发布列表)
    - [2.5.2 发布模块](#252-发布模块)
  - [2.6 模块四：能力开放](#26-模块四能力开放市场前端--独立部署)
    - [2.6.1 首页](#261-首页)
    - [2.6.2 能力详情](#262-能力详情)
    - [2.6.3 能力调试](#263-能力调试)
    - [2.6.4 登录用户信息](#264-登录用户信息)
- [3 技术架构](#3-技术架构)
  - [3.1 整体技术栈](#31-整体技术栈)
  - [3.2 工作流编排 引擎架构](#32-flowgramai-引擎架构)
    - [3.2.1 四引擎架构总览](#321-四引擎架构总览)
    - [3.2.2 四引擎职责详解](#322-四引擎职责详解)
    - [3.2.3 节点执行器模型（INodeExecutor）](#323-节点执行器模型inodeexecutor)
    - [3.2.4 当前支持的节点类型](#324-当前支持的节点类型)
    - [3.2.5 自定义节点扩展机制](#325-自定义节点扩展机制)
    - [3.2.6 节点扩展示例：SQL 查询节点](#326-节点扩展示例sql-查询节点)
    - [3.2.7 可扩展节点规划](#327-可扩展节点规划)
    - [3.2.8 插件体系](#328-插件体系)
  - [3.3 系统分层架构](#33-系统分层架构)
  - [3.4 Spring Boot 动态路由设计](#34-spring-boot-动态路由设计)
  - [3.5 FlowGram Runtime 对接](#35-flowgram-runtime-对接)
  - [3.6 编辑器与运行时集成](#36-编辑器与运行时集成)
- [4 对外调用方案](#4-对外调用方案)
  - [4.1 方案总览](#41-方案总览)
  - [4.2 流程发布](#42-流程发布)
  - [4.3 动态路由注册](#43-动态路由注册)
  - [4.4 API 接口定义规范](#44-api-接口定义规范)
  - [4.5 能力详情页 API 文档展示规范](#45-能力详情页-api-文档展示规范)
- [5 数据库设计](#5-数据库设计)
  - [5.1 核心表结构](#51-核心表结构)
  - [5.2 ER 关系](#52-er-关系)
- [6 部署与运维](#6-部署与运维)
  - [6.1 部署架构总体设计](#61-部署架构总体设计)
  - [6.2 水平扩展方案](#62-水平扩展方案)
  - [6.3 信创环境部署架构](#63-信创环境部署架构定开场景)
  - [6.4 关键运维指标](#64-关键运维指标)
  - [6.5 健康检查](#65-健康检查)
  - [6.6 日志规范](#66-日志规范)
  - [6.7 发布与变更管理](#67-发布与变更管理)
- [7 信创兼容与适配方案](#7-信创兼容与适配方案)
  - [7.1 信创适配总体策略](#71-信创适配总体策略)
  - [7.2 架构层面适配设计](#72-架构层面适配设计)
  - [7.3 数据库适配详细方案](#73-数据库适配详细方案关系型数据库)
  - [7.4 缓存中间件适配方案](#74-缓存中间件适配方案redis--信创缓存)
  - [7.5 应用服务器适配方案](#75-应用服务器适配方案tomcat--信创中间件)
  - [7.6 操作系统与 CPU 架构适配](#76-操作系统与-cpu-架构适配)
  - [7.7 信创适配定开工作量估算](#77-信创适配定开工作量估算)
  - [7.8 信创适配交付物清单](#78-信创适配交付物清单)
- [附录](#附录)

<div style="page-break-after: always;"></div>

---

# 1 方案概述

## 1.1 项目背景

  业务方需要一套 **能力服务编排引擎**，覆盖能力的完整生命周期：**接入 → 编排 → 发布 → 开放**。服务提供商将原子 API 能力接入平台，编排人员通过可视化工作流将多个原子能力组合编排为复合能力，发布到指定功能域目录下，最终通过独立的能力开放市场（可嵌入能力开放平台）供外部订阅、调试和调用。

系统包含两个独立前端：
- **管理前端**（能力接入 + 能力发布 + 能力编排）：面向服务提供商和平台运营人员
- **市场前端**（能力开放）：面向能力消费方，支持嵌入到能力开放平台

## 1.2 系统定位

| 维度 | 说明 |
|------|------|
| **系统定位** | 简化版能力中台（能力引擎），覆盖能力接入、编排、发布、开放全生命周期 |
| **底层引擎** | 工作流编排（字节跳动开源的可视化工作流/智能体编排平台） |
| **前端形态** | 管理前端（能力接入/发布/编排）+ 市场前端（能力开放，独立部署可嵌入） |
| **核心价值** | 能力接入 → 可视化编排 → 一键发布 → 市场开放 → 调用可观测 |
| **业务闭环** | 接入上架 → 目录管理 → 流程编排 → 版本发布 → 开放调试 → 网关调用 |

## 1.3 总体架构概览
<img width="1333" height="997" alt="image" src="https://github.com/user-attachments/assets/d3c82658-68f9-456d-9c4e-88e8a0fa8e02" />

```
┌─────────────────────────────────────────────────────────────────────────────────  ┐
│                          能力引擎平台 · 总体架构                                     │
├────────────────────────────────────┬────────────────────────────────────────────  ┤
│                                    │                                              │  
│      管理前端（能力接入/编排/发布）     │  市场前端（能力开放 · 可嵌入）                   │ 
│                                    │                                              │  
│  ┌────────┐ ┌────────┐ ┌────────┐  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────    ┐ │
│  │能力接入 │ │能力编排  │ │能力发布 │  │  │ 首页  │ │ 详情 │ │ 调试  │ │ 用户信息      │ │
│  └────────┘ └────────┘ └────────┘  │  └──────┘ └──────┘ └──────┘ └──────────    ┘ │
│                                    │                                              │  
├────────────────────────────────────┴────────────────────────────────────────────  ┤
│                                                                                   │  
│                  video-cso-mgr (Spring Boot · Nacos) · 能力服务管理                 │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌────────────────────────   ┐ │
│  │ 能力接入服务  │  │ 能力编排服务   │ │ 能力发布服务   │ │ 能力开放服务                │ │
│  │ · 上架管理   │  │ · 目录管理     │ │ · 发布列表    │ │ · 首页聚合                 │ │
│  │ · 文档管理   │  │ · 流程编排     │ │ · 发布审核    │ │ · 详情/文档                │ │
│  │ · 版本管理   │  │ · 运行日志     │ │ · 路由注册    │ │ · 在线调试                 │ │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘ └───────────┬────────────   ┘│
│         │                │                │                     │                │  
│  ┌──────▼────────────────▼────────────────▼─────────────────────▼────────────  ┐ │
│  │       video-cso-exec-engine (Nodejs · Nacos) · 流程调度引擎核心                 │ │
│  │             参数校验 · Schema加载 · 结果组装  · 动态路由  · 执行引擎               │ │
│  └───────────────────────────────┬────────────────────────────────────────────  ┘ │
│                                  │                                                │  
├──────────────────────────────────┼──────────────────────────────────────────────  ┤
│                                  │                                                │  
│  ┌──────────┐ ┌────────┐ ┌───────▼───────┐ ┌────────────┐                         │
│  │  TaurusDB   │ │ Redis  │ │   Nacos 2.x   │ │ 双网关入口   │                         │
│  │  元数据  │ │ 缓存   │ │     注册与发现   │ │视联|能力开放 │                          │
│  └──────────┘ └────────┘ └───────────────┘ └────────────┘                         │
│                                                                                   │  
└─────────────────────────────────────────────────────────────────────────────────  ┘
```

**核心闭环**：能力接入（原子 API 注册）→ 能力编排（可视化流程组合）→ 能力发布（上架到功能域目录）→ 能力开放（市场展示、调试、调用）

---

# 2 功能架构

## 2.1 业务闭环与模块总览

```
┌─────────────────────────────────────────────────────────────────────────────  ┐
│                   能力引擎平台 · 业务闭环                                        │
│                                                                               │  
│   ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────    ┐         │
│   │ 一、     │      │ 二、      │     │ 三、     │     │ 四、          │         │
│   │ 能力接入  │ ──→ │ 能力编排   │ ──→ │ 能力发布  │ ──→ │ 能力开放       │         │
│   │          │     │          │     │          │     │              │         │    
│   │ 上架管理  │     │ 目录管理   │     │ 发布列表  │     │ 首页          │         │
│   │ 文档管理  │     │ 流程编排   │     │ 发布模块  │     │ 能力详情       │         │
│   │ 版本管理  │     │ 运行日志   │     │          │     │ 能力调试       │         │
│   │          │     │          │     │          │     │ 用户信息       │         │
│   └──────────┘     └──────────┘     └──────────┘     └──────────    ┘          │
│                                                                                │  
│   ├─────── 管理前端（独立部署）────────┤              ├── 市场前端 ──┤              │
│   │     能力接入 + 能力编排 + 能力发布  │              │（独立部署    │              │
│   │     面向：服务提供商 / 平台运营     │              │  可嵌入）    │             │
│   └─────────────────────────────────────┘         └─────────────┘              │
│                                                                                │  
└─────────────────────────────────────────────────────────────────────────────  ┘
```

### 模块与前端归属

| 模块 | 所属前端 | 面向用户 | 核心职责 |
|------|---------|---------|---------|
| **能力接入** | 管理前端 | 服务提供商 / 平台运营 | 将原子 API 能力注册到平台，管理文档和版本 |
| **能力编排** | 管理前端 | 编排人员 | 在功能域目录下通过可视化工作流组合编排复合能力 |
| **能力发布** | 管理前端 | 编排人员 / 审核人员 | 将编排好的能力发布到指定功能域目录，管理发布状态 |
| **能力开放** | 市场前端（独立部署，可嵌入能力开放平台） | 能力消费方 / 第三方开发者 | 浏览、查看详情、在线调试、获取调用凭证 |

## 2.2 用户角色

| 角色 | 所属前端 | 典型场景 |
|------|---------|----------|
| **服务提供商** | 管理前端 | 将已有的原子 API（如告警查询、设备管理等）接入上架，维护文档和版本 |
| **编排人员** | 管理前端 | 在能力目录下创建编排流程，通过拖拽节点组合多个原子能力为复合能力 |
| **平台运营** | 管理前端 | 管理能力目录（功能域），审核发布申请，查看运行日志 |
| **能力消费方** | 市场前端 | 在首页浏览已发布能力，查看详情文档，在线调试验证 |
| **第三方开发者** | 市场前端 / HTTP 调用 | 通过市场获取 API 文档和凭证，在业务系统中通过网关调用 |

---

## 2.3 模块一：能力接入（管理前端）

> 服务提供商将原子 API 能力注册到平台，作为编排的「原材料」。

### 2.3.1 能力接入上架

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-A01 | 能力列表 | 分页展示已接入能力（名称、提供商、状态、版本、更新时间），支持搜索/筛选 |
| F-A02 | 新增能力 | 填写能力基本信息（名称、描述、分类、提供商）、调用地址、请求方式 |
| F-A03 | 编辑能力 | 修改能力基本信息、调用配置 |
| F-A04 | 删除能力 | 二次确认后删除（已被编排引用的能力不可删除） |
| F-A05 | 上架/下架 | 控制能力是否可被编排模块引用；下架后已引用的编排不受影响 |
| F-A06 | 能力分类管理 | 按业务域分类（如：告警、设备、视频、安全等） |

### 2.3.2 能力文档管理

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-A07 | 请求头定义 | 配置 Headers 参数（名称、类型、必填、说明），如 Content-Type、Authorization |
| F-A08 | 入参定义 | 配置 Query / Body 参数（名称、类型、必填、默认值、说明、示例值） |
| F-A09 | 出参定义 | 配置响应 Body 结构（字段名、类型、说明），支持嵌套对象/数组 |
| F-A10 | 请求示例 | 录入完整的请求示例（cURL / JSON），可自动从入参生成 |
| F-A11 | 响应示例 | 录入成功/失败的响应示例 JSON |
| F-A12 | 错误码说明 | 配置该能力特有的错误码及含义 |
| F-A13 | 文档预览 | 以标准 API 文档格式预览（类似 Swagger UI 风格） |

### 2.3.3 能力版本管理

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-A14 | 版本列表 | 按能力查看所有版本（版本号、状态、发布时间、域标识） |
| F-A15 | 新增版本 | 基于已有版本创建新版本，继承文档和配置，支持修改 |
| F-A16 | 域隔离 | 区分 **属地域** 和 **中心域**，不同域下版本独立管理 |
| F-A17 | 版本状态 | 草稿 → 已发布 → 已废弃；同一域下仅一个版本为「已发布」 |
| F-A18 | 版本对比 | 对比两个版本的参数差异（入参/出参变更） |
| F-A19 | 版本切换 | 将指定版本设为当前生效版本（编排中引用自动指向最新生效版本） |

---

## 2.4 模块二：能力编排（管理前端）

> 在功能域目录下，通过可视化工作流将多个原子能力组合编排为复合能力。

### 2.4.1 能力目录（功能域）

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-B01 | 目录树管理 | 多级功能域目录（如：交通管理/告警处理、设备管控/状态查询），支持增删改 |
| F-B02 | 目录排序 | 拖拽调整目录顺序 |
| F-B03 | 目录关联 | 编排流程归属到指定目录节点下 |
| F-B04 | 目录统计 | 展示每个目录下的编排数量、已发布数量 |

### 2.4.2 能力编排模块

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-B05 | 编排列表 | 按目录筛选，展示编排流程（名称、状态、版本、更新时间） |
| F-B06 | 新增编排 | 创建编排并选择归属目录，进入可视化编辑器 |
| F-B07 | 画布编辑 | 基于 工作流编排 的自由布局画布，拖拽节点、连线、配置 |
| F-B08 | 节点类型 | 开始、结束、HTTP（调用已接入能力）、LLM、代码、条件、循环、变量 |
| F-B09 | 引用已接入能力 | HTTP 节点可从「能力接入」模块选择已上架能力，自动填充地址和参数结构 |
| F-B10 | 节点表单配置 | 配置每个节点的参数（支持变量引用上游节点输出） |
| F-B11 | 变量面板 | 全局变量管理，作用域链可视化 |
| F-B12 | 保存 | 防抖自动保存 design_json |
| F-B13 | 测试运行 | 在编辑器内填写 inputs 执行测试，查看每个节点的输入/输出/耗时 |
| F-B14 | 复制编排 | 复制为新编排（名称带后缀），含完整 design_json |
| F-B15 | 删除编排 | 已发布的编排需先下架才能删除 |

### 2.4.3 运行日志

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-B16 | 执行记录列表 | 按编排流程查看执行历史（任务ID、状态、来源、耗时） |
| F-B17 | 状态筛选 | 按 running / success / failed / cancelled 筛选 |
| F-B18 | 执行详情 | 查看输入参数、输出结果、节点级日志、错误信息 |
| F-B19 | 来源区分 | 区分编辑器测试（page）、API 调用（api）、网关调用（gateway） |

---

## 2.5 模块三：能力发布（管理前端）

> 将编排好的复合能力发布到指定功能域目录，注册动态路由，使其可被外部调用。

### 2.5.1 发布列表

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-C01 | 发布列表 | 展示所有发布记录（能力名称、目录、版本、状态、发布时间） |
| F-C02 | 状态管理 | 三种状态：**待发布** / **已发布** / **已取消** |
| F-C03 | 搜索筛选 | 按名称、目录、状态、版本筛选 |
| F-C04 | 发布详情 | 查看完整发布信息：API 地址、参数、版本、发布人、发布时间 |
| F-C05 | 取消发布 | 注销动态路由，标记为已取消（保留历史记录） |
| F-C06 | 重新发布 | 对已取消的记录重新发布，重新注册路由 |
| F-C07 | 删除记录 | 仅可删除「待发布」和「已取消」状态的记录 |

### 2.5.2 发布模块

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-C08 | 选择编排 | 从编排列表中选择要发布的流程 |
| F-C09 | 选择目标目录 | 发布到指定的功能域目录下 |
| F-C10 | 服务级别选择 | 选择 serviceTier（core / general），决定路由到哪个集群 |
| F-C11 | 参数自动提取 | 从编排的 start 节点自动提取 inputs，推断类型和默认值 |
| F-C12 | 参数元数据编辑 | 调整参数名、类型、必填、默认值、描述 |
| F-C13 | 响应结构定义 | 录入响应 Body 的 JSON 结构及字段说明 |
| F-C14 | 路由地址生成 | 自动生成 `/video_cso_exec_engine/api/flow/{serviceTier}/{flowCode}` |
| F-C16 | 版本号 | 填写版本号（如 1.0.0），同一 flowCode 支持多版本 |
| F-C17 | 发布确认 | 校验 → 注册动态路由 → 写入发布记录 → Schema 加密存储 |

---

## 2.6 模块四：能力开放（市场前端 · 独立部署）

> 独立的市场前端，支持嵌入到能力开放平台，面向能力消费方提供浏览、调试、调用能力。

### 2.6.1 首页

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-D01 | 能力搜索 | 全局搜索框，按关键词搜索已发布能力 |
| F-D02 | 分类导航 | 按功能域目录分类展示能力卡片 |
| F-D03 | 热门能力 | 展示调用量 Top N 的热门能力 |
| F-D04 | 最新上架 | 展示最近发布的能力，按发布时间倒序 |
| F-D05 | 能力卡片 | 每个能力展示：名称、描述、分类、版本、提供商、调用量 |
| F-D06 | 公告/通知 | 平台公告、能力变更通知 |

### 2.6.2 能力详情

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-D07 | API 接口信息 | 展示调用地址、请求方式、返回类型 |
| F-D08 | 鉴权说明 | 展示支持的鉴权方式（Token / AccessKey）及使用方法 |
| F-D09 | 请求参数文档 | 展示 Headers、Query、Body 参数（名称/类型/必填/说明） |
| F-D10 | 响应参数文档 | 展示响应结构（字段名/类型/说明），含嵌套结构 |
| F-D11 | 请求/响应示例 | 展示请求和响应的完整 JSON 示例 |
| F-D12 | 错误码说明 | 展示通用错误码 + 能力特有错误码 |
| F-D13 | 版本切换 | 在详情页切换查看不同版本的文档 |
| F-D14 | 使用指南 | 可附加使用指南文档（Markdown 或文件下载） |

### 2.6.3 能力调试

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-D15 | 参数填写 | 根据入参定义自动生成表单，支持手动编辑 JSON |
| F-D16 | 自动携带鉴权 | 登录用户自动携带调试凭证 |
| F-D17 | 发起调用 | 点击「发送」按钮发起真实 HTTP 调用 |
| F-D18 | 请求/响应展示 | 展示完整的请求头、请求体、响应头、响应体、状态码、耗时 |
| F-D19 | 历史记录 | 保存调试历史（最近 N 次），可快速重放 |

### 2.6.4 登录用户信息

| 序号 | 功能项 | 描述 |
|------|--------|------|
| F-D20 | SSO 对接 | 对接统一认证中心（SSO），从能力开放平台免登录跳转 |
| F-D21 | 用户基本信息 | 展示当前登录用户名称、所属组织 |
| F-D23 | 我的调用统计 | 查看个人/组织的调用量、成功率 |

---

# 3 技术架构
<img width="3342" height="1330" alt="image" src="https://github.com/user-attachments/assets/291e8c43-5984-494b-8100-cbead43a7642" />

## 3.1 整体技术栈

| 层次 | 默认技术选型 | 信创适配选型（定开） | 说明 |
|------|------------|-------------------|------|
| **前端** | React + TypeScript + Rsbuild | — | 纯浏览器端，无信创适配需求 |
| **编辑器引擎** | @工作流编排/free-layout-editor | — | 纯前端组件 |
| **运行时（Node.js）** | @工作流编排/runtime-js + Express | — | FlowGram 执行引擎 |
| **发布与路由服务（Java）** | Spring Boot 2.7+ / Spring Cloud | — | 动态路由、API 元数据、Nacos 注册 |
| **微服务注册** | Nacos 2.x | — | 服务注册与发现、配置中心 |
| **API 网关** | 视联平台网关 + 能力开放网关 | — | 双入口网关 |
| **数据库** | TaurusDB 8.0 | 达梦 DM8 / 人大金仓 KingbaseES / 南大通用 GBase / 瀚高 HighGo（**需定开**） | 详见[第 7 章 信创兼容方案](#7-信创兼容与适配方案) |
| **缓存** | Redis 6.x | 东方通 TongRDS / 中创中间件 Redis（**需定开**） | 详见[第 7 章 信创兼容方案](#7-信创兼容与适配方案) |
| **应用服务器** | Tomcat (Spring Boot 内嵌) | 东方通 TongWeb / 中创 InforSuite AS（**需定开**） | 详见[第 7 章 信创兼容方案](#7-信创兼容与适配方案) |
| **操作系统** | CentOS / Ubuntu | 银河麒麟 / 统信 UOS / 中标麒麟 | 需验证兼容性 |
| **CPU 架构** | x86_64 | 鲲鹏 ARM64 / 飞腾 ARM64 / 龙芯 LoongArch | JDK/Node.js 需适配对应架构 |

## 3.2 工作流编排 引擎架构

工作流编排 采用四引擎分层 + 插件化的技术架构。理解其内部机制是后续扩展业务节点（如 SQL 节点、消息队列节点等）的基础。

### 3.2.1 四引擎架构总览

```
┌────────────────────────────────────────────────────────────────────────────────  ┐
│                       工作流编排 引擎架构                                        │
├────────────────────────────────────────────────────────────────────────────────  │
│                                                                                  │  
│  ┌─ 画布引擎 (Canvas Engine) ───────────────────────────────────────────────  ┐   │
│  │                                                                           │  │  
│  │  Playground (坐标系/交互)  →  Layer (分层渲染)  →  FlowNodeEntity (节点)      │  │
│  │                                                                           │  │  
│  │  布局模式: FreeLayout (自由布局) | FixedLayout (固定布局)                     │  │
│  │  采用 ECS 架构，Entity-Component-System 分离数据与渲染                        │  │
│  └──────────────────────────────────────────────────────────────────────────  ┘ │
│       │ 提供画布节点实体                                                           │
│       ▼                                                                         │
│  ┌─ 节点引擎 (Node Engine) ─────────────────────────────────────────────────  ┐  │
│  │                                                                            │  │  
│  │  FlowNodeRegistry ──→ FormMeta (静态配置)                                   │  │
│  │       │                    │                                               │  │ 
│  │       ▼                    ▼                                               │  │
│  │  FormModel (数据模型)  →  Form (渲染/验证/副作用)  →  Field (表单字段)          │  │
│  │       │                                                                    │  │ 
│  │       └──→ FormPlugin (插件扩展: 变量绑定 / 自定义校验 / 联动逻辑)               │  │
│  │                                                                            │  │  
│  │  核心特性: 数据与渲染分离、插件化扩展、副作用管理                                  │  │
│  └──────────────────────────────────────────────────────────────────────────  ┘  │
│       │ 提供节点表单数据                                                            │
│       ▼                                                                          │
│  ┌─ 变量引擎 (Variable Engine) ─────────────────────────────────────────────  ┐   │ 
│  │                                                                            │  │  
│  │  Scope (作用域)  →  Variable (变量实例)  →  AST (类型描述树)                   │  │
│  │                                                                            │  │  
│  │  基于依赖倒置原则，通过作用域链管理变量:                                          │  │
│  │  - 全局变量: 工作流级别共享                                                    │  │
│  │  - 节点输出变量: 下游节点可通过作用域链引用上游输出                                │  │
│  │  - 循环变量: 循环容器内的迭代变量                                               │  │
│  └──────────────────────────────────────────────────────────────────────────  ┘  │
│       │ 提供变量上下文                                                             │
│       ▼                                                                          │
│  ┌─ 运行时引擎 (Runtime Engine) ────────────────────────────────────────────  ┐    │
│  │                                                                           │   │  
│  │  WorkflowEngine (调度核心)                                                  │  │
│  │       │                                                                    │  │ 
│  │       ├──→ SchemaParser (Schema 解析 → DAG 构建)                            │  │
│  │       ├──→ ExecutionEngine (按 DAG 顺序调度节点执行)                          │  │
│  │       ├──→ NodeExecutorRegistry (节点执行器注册表)                            │  │
│  │       │         │                                                          │  │ 
│  │       │         ├── StartExecutor                                          │  │
│  │       │         ├── EndExecutor                                            │  │
│  │       │         ├── LLMExecutor                                            │  │
│  │       │         ├── ConditionExecutor                                      │  │
│  │       │         ├── LoopExecutor                                           │  │
│  │       │         ├── HTTPExecutor (本仓库扩展)                                │  │
│  │       │         ├── CodeExecutor (本仓库扩展)                                │  │
│  │       │         ├── VariableExecutor (本仓库 patch)                          │  │
│  │       │         └── ★ 自定义 Executor (SQL/MQ/... 按需扩展)                   │  │
│  │       │                                                                     │  │ 
│  │       ├──→ StateManager (任务状态管理)                                        │  │
│  │       └──→ WorkflowValidator (Schema 校验)                                   │  │
│  │                                                                             │  │  
│  │  DDD 三层架构: Application → Domain → Infrastructure                         │  │
│  └──────────────────────────────────────────────────────────────────────────  ┘   │
│                                                                                   │  
├─ 插件体系 (23+ 内置插件) ──────────────────────────────────────────────────────     │
│  FreeLinesPlugin    | FreeSnapPlugin     | MinimapPlugin    | HistoryPlugin      │
│  FreeNodePanelPlugin| ContainerNodePlugin| ContextMenuPlugin| VariablePanel      │
│  RuntimePlugin      | PanelManagerPlugin | FreeGroupPlugin  | ...                │
└────────────────────────────────────────────────────────────────────────────────  ┘
```

### 3.2.2 四引擎职责详解

| 引擎 | 包名 | 职责 | 核心概念 |
|------|------|------|---------|
| **画布引擎** | `@工作流编排/canvas-engine` | 画布渲染、坐标系、交互、分层渲染 | Playground、Layer、FlowNodeEntity、ECS 架构 |
| **节点引擎** | `@工作流编排/node-engine` | 节点数据管理、表单配置、校验、副作用 | FlowNodeRegistry、FormMeta、FormModel、Form、Field、FormPlugin |
| **变量引擎** | `@工作流编排/variable-engine` | 变量作用域链、类型推断、变量引用 | Scope、Variable、AST、依赖倒置 |
| **运行时引擎** | `@工作流编排/runtime-js` | 工作流解析、DAG 调度、节点执行、状态管理 | WorkflowEngine、ExecutionEngine、INodeExecutor、StateManager |

### 3.2.3 节点执行器模型（INodeExecutor）

每个节点类型对应一个 `INodeExecutor` 实现，遵循标准三阶段执行生命周期：

```
┌───────────────────────────────────────────────────────────────────  ┐
│                    INodeExecutor 执行生命周期                         │
├───────────────────────────────────────────────────────────────────  ┤
│                                                                     │  
│  ┌─ 1. 准备阶段 (Prepare) ─────────────────────────────────────   ┐  │
│  │  · 从 ExecutionContext 获取输入数据                              │ │
│  │  · 输入参数校验                                                  │ │
│  │  · 前置条件检查                                                  │ │
│  └─────────────────────────────────────────────────────────────   ┘ │
│                          │                                          │  
│                          ▼                                          │
│  ┌─ 2. 执行阶段 (Execute) ────────────────────────────────────   ┐   │
│  │  · 执行节点特定的业务逻辑                                        │   │
│  │  · 异常捕获与处理                                               │  │
│  │  · 支持异步操作（HTTP调用、DB查询等）                              │  │
│  └─────────────────────────────────────────────────────────────   ┘ │
│                          │                                          │  
│                          ▼                                          │
│  ┌─ 3. 完成阶段 (Complete) ───────────────────────────────────   ┐   │
│  │  · 输出结果写入 ExecutionContext                                │  │
│  │  · 更新节点状态（success / failed）                              │  │
│  │  · 生成节点日志                                                 │  │
│  └─────────────────────────────────────────────────────────────   ┘ │
│                                                                     │  
└───────────────────────────────────────────────────────────────────  ┘
```

**INodeExecutor 接口定义**：

```typescript
interface INodeExecutor {
  /** 节点类型标识，如 'start', 'end', 'llm', 'http', 'sql' */
  type: string;

  /** 核心执行方法 */
  execute(context: ExecutionContext): Promise<ExecutionResult>;
}
```

### 3.2.4 当前支持的节点类型

| 节点类型 | 类型标识 | Executor | 来源 | 用途 |
|---------|---------|----------|------|------|
| 开始 | `start` | StartExecutor | 官方内置 | 工作流入口，接收 inputs |
| 结束 | `end` | EndExecutor | 官方内置 | 工作流出口，输出 outputs |
| LLM | `llm` | LLMExecutor | 官方内置 | 调用大语言模型 |
| 条件 | `condition` | ConditionExecutor | 官方内置 | 分支判断，控制执行流 |
| 循环 | `loop` | LoopExecutor | 官方内置 | 迭代执行子画布 |
| HTTP | `http` | HTTPExecutor | **本仓库扩展** | 发送 HTTP 请求 |
| 代码 | `code` | CodeExecutor | **本仓库扩展** | 执行 JS/TS 代码片段 |
| 变量 | `variable` | VariableExecutor | **本仓库 patch** | 读写全局变量 |

### 3.2.5 自定义节点扩展机制

扩展新节点类型需要完成 **前端（编辑器侧）** 和 **后端（运行时侧）** 两部分工作：

```
┌─────────────────────────────────────────────────────────────────────────────  ┐
│                     自定义节点扩展全流程（以 SQL 节点为例）                         │
├─────────────────────────────────────────────────────────────────────────────  ┤
│                                                                               │  
│  ┌─ 前端：编辑器侧 ──────────────────────────────────────────────────────   ┐   │
│  │                                                                          │ │  
│  │  1. 定义节点注册信息 (FlowNodeRegistry)                                    │ │
│  │     ┌──────────────────────────────────────────────────  ┐               │ │
│  │     │ type: 'sql'                                        │               │ │
│  │     │ meta: { name: 'SQL查询', icon: 'database' }         │               │ │
│  │     │ formMeta: { ... }  ← 配置表单结构                    │               │ │
│  │     └──────────────────────────────────────────────────  ┘               │ │
│  │                                                                          │ │  
│  │  2. 定义节点表单 (FormMeta)                                                │ │
│  │     · 数据源选择（下拉框）                                                  │ │
│  │     · SQL 语句编辑器（代码编辑框）                                          │ │
│  │     · 参数绑定（变量选择器，引用上游变量）                                    │ │
│  │     · 结果映射（输出字段配置）                                              │ │
│  │                                                                         │ │  
│  │  3. 定义节点渲染组件 (React Component)                                     │ │
│  │     · 画布上的节点外观                                                     │ │
│  │     · 侧边栏配置面板                                                       │ │
│  │                                                                          │ │  
│  └────────────────────────────────────────────────────────────────────────  ┘ │
│                                                                               │  
│  ┌─ 后端：运行时侧 ──────────────────────────────────────────────────────     ┐ │
│  │                                                                          │ │  
│  │  4. 实现节点执行器 (INodeExecutor)                                         │ │
│  │     ┌──────────────────────────────────────────────────  ┐               │ │
│  │     │ class SQLExecutor implements INodeExecutor {       │               │ │
│  │     │   type = 'sql';                                    │               │ │
│  │     │   async execute(ctx) {                             │               │ │
│  │     │     // 1. 从 ctx 获取数据源配置 & SQL                 │               │ │
│  │     │     // 2. 执行 SQL 查询                              │               │ │
│  │     │     // 3. 返回查询结果                               │               │ │
│  │     │   }                                                │               │ │
│  │     │ }                                                  │               │ │
│  │     └──────────────────────────────────────────────────  ┘               │ │
│  │                                                                          │ │  
│  │  5. 注册到 NodeExecutorRegistry                                           │ │
│  │     runtime.registerExecutor(new SQLExecutor());                         │ │
│  │                                                                          │ │  
│  └────────────────────────────────────────────────────────────────────────  ┘ │
│                                                                               │  
└─────────────────────────────────────────────────────────────────────────────  ┘
```

### 3.2.6 节点扩展示例：SQL 查询节点

**前端 — 节点注册与表单定义**：

```typescript
// src/nodes/sql/index.ts
export const sqlNodeRegistry: FlowNodeRegistry = {
  type: 'sql',
  meta: {
    name: 'SQL 查询',
    description: '执行 SQL 语句并返回结果集',
    icon: 'DatabaseOutlined',
    category: '数据',
  },
  formMeta: {
    fields: [
      {
        name: 'datasource',
        type: 'select',
        label: '数据源',
        required: true,
        options: [],             // 运行时从配置中心加载
      },
      {
        name: 'sql',
        type: 'code',
        label: 'SQL 语句',
        required: true,
        language: 'sql',
        placeholder: 'SELECT * FROM table WHERE id = {{input.id}}',
      },
      {
        name: 'params',
        type: 'variableBinding',
        label: '参数绑定',
        description: 'SQL 中 {{}} 引用的变量映射',
      },
      {
        name: 'resultMapping',
        type: 'keyValue',
        label: '结果映射',
        description: '将查询结果列映射为输出变量',
      },
    ],
  },
};
```

**后端 — 节点执行器实现**：

```typescript
// runtime/executors/sql-executor.ts
import { INodeExecutor, ExecutionContext, ExecutionResult } from '@工作流编排/runtime-interface';
import { createPool } from 'TaurusDB2/promise';

export class SQLExecutor implements INodeExecutor {
  type = 'sql';

  private pools: Map<string, any> = new Map();

  async execute(context: ExecutionContext): Promise<ExecutionResult> {
    // 1. 准备阶段：获取节点配置
    const { datasource, sql, params } = context.nodeData;

    // 2. 执行阶段：执行 SQL
    const pool = await this.getPool(datasource);
    const resolvedSQL = this.resolveVariables(sql, params, context);
    const [rows] = await pool.execute(resolvedSQL);

    // 3. 完成阶段：返回结果
    return {
      status: 'success',
      outputs: {
        rows: rows,
        rowCount: rows.length,
      },
    };
  }

  private resolveVariables(sql: string, params: any, ctx: ExecutionContext): string {
    return sql.replace(/\{\{(\w+(?:\.\w+)*)\}\}/g, (_, path) => {
      return ctx.getVariable(path) ?? '';
    });
  }

  private async getPool(datasource: string) {
    if (!this.pools.has(datasource)) {
      const config = await this.loadDatasourceConfig(datasource);
      this.pools.set(datasource, createPool(config));
    }
    return this.pools.get(datasource);
  }
}
```

**注册执行器**：

```typescript
// runtime/index.ts
import { SQLExecutor } from './executors/sql-executor';

runtime.registerExecutor(new SQLExecutor());
```

### 3.2.7 可扩展节点规划

| 节点类型 | 类型标识 | 优先级 | 用途 | 扩展要点 |
|---------|---------|-------|------|---------|
| **SQL 查询** | `sql` | P0 | 执行 SQL 查询/更新 | 数据源管理、SQL 注入防护、结果集映射 |
| **Redis 操作** | `redis` | P1 | 缓存读写 | 支持 GET/SET/DEL/HASH 等操作 |
| **消息队列** | `mq` | P1 | 发送/消费消息 | Kafka/RabbitMQ/RocketMQ 适配 |
| **数据转换** | `transform` | P1 | JSONPath / 字段映射 / 格式转换 | 通用数据加工 |
| **邮件通知** | `email` | P2 | 发送邮件通知 | SMTP 配置、模板渲染 |
| **定时触发** | `cron` | P2 | 定时启动工作流 | Cron 表达式、时区配置 |
| **子流程** | `subflow` | P2 | 嵌套调用其他已发布流程 | 递归防护、参数透传 |
| **审批** | `approval` | P3 | 人工审批节点（异步等待） | 审批人配置、超时处理、回调 |
| **文件操作** | `file` | P3 | 文件读写 / FTP / OSS | 多存储后端适配 |

### 3.2.8 插件体系

FlowGram 采用 IoC（控制反转）容器驱动的插件机制，每个插件遵循统一的生命周期：

| 生命周期钩子 | 触发时机 | 典型用途 |
|------------|---------|---------|
| `onBind` | IoC 模块注册时 | 注册依赖、声明服务 |
| `onInit` | 画布初始化时 | 初始化插件状态 |
| `onReady` | DOM 就绪时 | 绑定事件、注册快捷键 |
| `onAllLayersRendered` | 所有层渲染完成后 | 后处理回调 |
| `onDispose` | 画布销毁时 | 清理资源、解绑事件 |

**内置插件清单（23+）**：

| 类别 | 插件 | 功能 |
|------|------|------|
| 连线与布局 | FreeLinesPlugin、FreeSnapPlugin、FreeGroupPlugin | 连线管理、对齐辅助线、分组 |
| 交互增强 | ContextMenuPlugin、MinimapPlugin、FreeNodePanelPlugin | 右键菜单、缩略图、节点面板 |
| 数据管理 | HistoryPlugin、VariablePanelPlugin | 撤销/重做、变量面板 |
| 容器与嵌套 | ContainerNodePlugin | 循环/分组容器子画布 |
| 面板管理 | PanelManagerPlugin | 侧边栏面板自动布局与队列管理 |
| 运行时 | RuntimePlugin | 运行按钮、状态展示、日志查看 |

## 3.3 系统分层架构

```
┌───────────────────────────────────────────────────────────────────────────────    ┐
│                              接入层 (Gateway)                                      │
│   视联平台网关(Token鉴权)              能力开放网关(AccessKey签名)                     │
│                                                                                   │    
│   路由规则（按 URL 路径中的 serviceTier 分发到对应独立服务）：                            │
│   /video_cso_exec_engine/api/flow/core/**    → lb://video-cso-exec-engine-core    │
│   /video_cso_exec_engine/api/flow/general/** → lb://video-cso-exec-engine-general │
│   /video_cso_exec_engine/api/flow/realtime/**→ lb://video-cso-exec-engine-realtime│
└──────────────────────┬────────────────────────────┬───────────────────────────    ┘
                       │ core 流量                   │ general 流量                  
                       ▼                             ▼
      ┌─────────────────────────────┐  ┌─────────────────────────────               ┐
      │ video-cso-exec-engine-core  │  │ video-cso-exec-engine-general              │
      │   (独立服务名)                │  │ (独立服务名)                                │
      │ Nacos 服务名:                │  │ Nacos 服务名:                               │
      │ video-cso-exec-engine-core  │  │   video-cso-exec-engine-general            │
      │ Replicas: 3~20 (HPA)        │  │ Replicas: 2~10 (HPA)                       │
      └────────────┬────────────────┘  └──────────────┬───────────────              ┘
                   │                                  │                              
      ┌────────────▼──────────────────────────────────▼───────────────              ┐
      │                   流程调度引擎核心（共享逻辑）                                   │
      │  POST /video_cso_exec_engine/api/flow/{serviceTier}/{flowCode}              │
      │  - 参数校验 & 类型转换                                                         │
      │  - Schema 加载（DB + Redis 缓存）                                             │
      │  - 调用 agents-server（FlowGram Runtime）执行                                 │
      │  - 结果组装 & 统一响应                                                         │
      └────────────────────────────┬──────────────────────────────────              ┘
                                   │ HTTP (RestTemplate / WebClient)                 
      ┌────────────────────────────▼──────────────────────────────────              ┐
      │               agents-server (Node.js + Express)                             │
      │  Runtime-Engine 执行引擎                                                │
      │  - POST /api/task/run     启动工作流                                          │
      │  - GET  /api/task/report  获取中间状态                                        │
      │  - GET  /api/task/result  获取最终结果                                        │
      │  - PUT  /api/task/cancel  取消任务                                           │
      │  - POST /api/task/validate 校验                                             │
      └──────────────────────────────────────────────────────────────               ┘
```

> **核心设计**：不同服务级别以独立的 Nacos 服务名注册（`video-cso-exec-engine-core`、`video-cso-exec-engine-general`、`smart-flow-engine-realtime`）。网关根据请求 URL 中的 `serviceTier` 路径段，将流量路由到对应的独立服务，实现 **同一套代码、独立服务名、独立资源隔离** 的效果。后续扩展新的服务级别只需以新服务名部署实例 + 网关增加路由规则，无需修改代码。

## 3.4 Spring Boot 动态路由设计

### 3.4.1 设计目标

流程发布后，无需重启服务即可注册新的 HTTP 端点，外部通过统一路径模式调用。

### 3.4.2 路由模式

```
统一上下文:      /video_cso_exec_engine
路由模式:        /video_cso_exec_engine/api/flow/{serviceTier}/{flowCode}
serviceTier 值:  core | general | 自定义扩展（如 realtime、batch）
HTTP 方法:       POST（默认）
Content-Type:    application/json

示例:
  核心服务:  POST /video_cso_exec_engine/api/flow/core/warning
  一般服务:  POST /video_cso_exec_engine/api/flow/general/report-export
```

`serviceTier` 承担两个职责：
1. **网关侧**：作为路由分发依据，不同 tier 的请求路由到不同的后端实例组
2. **服务侧**：作为路由查询的过滤条件，确保 flowCode 在对应 tier 下唯一

### 3.4.3 实现方案：统一入口 + 服务级别路由分发

采用 **统一 Controller 入口 + serviceTier 路径参数 + 数据库路由表** 的方式：

```java
@RestController
@RequestMapping("/video_cso_exec_engine/api/flow")
public class FlowDispatchController {

    @Autowired
    private FlowRouteService flowRouteService;

    @Autowired
    private FlowExecutionService flowExecutionService;

    /**
     * 统一流程调用入口
     * 根据 serviceTier + flowCode 路由到对应的已发布流程
     *
     * @param serviceTier 服务级别标签: core / general / 自定义
     * @param flowCode    流程编码
     */
    @PostMapping("/{serviceTier}/{flowCode}")
    public ResponseEntity<FlowApiResponse> execute(
            @PathVariable String serviceTier,
            @PathVariable String flowCode,
            @RequestBody(required = false) Map<String, Object> body,
            @RequestParam Map<String, String> queryParams,
            HttpServletRequest request) {

        // 1. 校验 serviceTier 合法性
        if (!flowRouteService.isValidTier(serviceTier)) {
            return ResponseEntity.badRequest()
                .body(FlowApiResponse.fail(400, "无效的服务级别: " + serviceTier));
        }

        // 2. 查找已发布的流程路由（按 tier + flowCode 联合查询）
        FlowRoute route = flowRouteService.getActiveRoute(serviceTier, flowCode);
        if (route == null) {
            return ResponseEntity.status(404)
                .body(FlowApiResponse.fail(404, "流程能力不存在或已下架"));
        }

        // 3. 参数校验
        ValidationResult validation = flowRouteService.validateParams(route, body, queryParams);
        if (!validation.isValid()) {
            return ResponseEntity.badRequest()
                .body(FlowApiResponse.fail(400, validation.getMessage()));
        }

        // 4. 组装 inputs 并执行
        Map<String, Object> inputs = flowRouteService.assembleInputs(route, body, queryParams);
        FlowExecutionResult result = flowExecutionService.executeFlow(route, inputs, request);

        // 5. 返回统一响应
        return ResponseEntity.ok(FlowApiResponse.success(result.getOutputs()));
    }
}
```

### 3.4.4 路由缓存策略

缓存 Key 包含 `serviceTier`，确保不同级别的同名 flowCode 不冲突：

```java
@Service
public class FlowRouteService {

    @Autowired
    private FlowRouteRepository flowRouteRepo;

    @Autowired
    private RedisTemplate<String, FlowRoute> redisTemplate;

    private static final String ROUTE_CACHE_PREFIX = "smart:flow:route:";
    private static final long CACHE_TTL_MINUTES = 30;

    private static final Set<String> VALID_TIERS = Set.of("core", "general");
    // 扩展时在此处或配置中心动态加载

    public boolean isValidTier(String tier) {
        return VALID_TIERS.contains(tier);
    }

    public FlowRoute getActiveRoute(String serviceTier, String flowCode) {
        String cacheKey = ROUTE_CACHE_PREFIX + serviceTier + ":" + flowCode;
        FlowRoute cached = redisTemplate.opsForValue().get(cacheKey);
        if (cached != null) {
            return cached;
        }
        FlowRoute route = flowRouteRepo
            .findByServiceTierAndFlowCodeAndStatus(serviceTier, flowCode, "ACTIVE");
        if (route != null) {
            redisTemplate.opsForValue().set(cacheKey, route, CACHE_TTL_MINUTES, TimeUnit.MINUTES);
        }
        return route;
    }

    public void invalidateRouteCache(String serviceTier, String flowCode) {
        redisTemplate.delete(ROUTE_CACHE_PREFIX + serviceTier + ":" + flowCode);
    }
}
```

### 3.4.5 Nacos 注册配置

每个服务级别以独立的 Nacos 服务名注册。通过启动参数 `SERVICE_TIER` 拼接服务名 `smart-flow-engine-${SERVICE_TIER}`，网关直接按服务名路由，无需元数据过滤：

```yaml
# application.yml — 公共配置
spring:
  application:
    name: smart-flow-engine-${SERVICE_TIER:general}
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
        namespace: ${NACOS_NAMESPACE:public}
        group: DEFAULT_GROUP
        metadata:
          version: 1.0.0
      config:
        server-addr: ${NACOS_SERVER:127.0.0.1:8848}
        file-extension: yaml

server:
  port: 8080
  servlet:
    context-path: /video_cso_exec_engine

# agents-server 连接配置
flowgram:
  agents-server:
    base-url: http://agents-server:4000
    connect-timeout: 5000
    read-timeout: 60000
    max-connections: 200
```

**部署时通过环境变量指定服务级别，服务名自动拼接**：

```bash
# 核心集群实例 → 注册为 video-cso-exec-engine-core
SERVICE_TIER=core java -jar smart-flow-engine.jar

# 一般集群实例 → 注册为 video-cso-exec-engine-general
SERVICE_TIER=general java -jar smart-flow-engine.jar

# 未来扩展：实时集群 → 注册为 smart-flow-engine-realtime
SERVICE_TIER=realtime java -jar smart-flow-engine.jar
```

在 Nacos 控制台中，可看到三个独立服务：

```
video-cso-exec-engine-core      (Instance-1, Instance-2, Instance-3, ...)
video-cso-exec-engine-general   (Instance-1, Instance-2, ...)
smart-flow-engine-realtime  (扩展时新增)
```

### 3.4.6 网关路由配置（核心）

网关根据请求路径中的 `serviceTier` 段，将流量路由到对应的独立 Nacos 服务。以 Spring Cloud Gateway 为例：

```yaml
# 网关路由配置
spring:
  cloud:
    gateway:
      routes:
        # 核心服务路由 → 转发到 video-cso-exec-engine-core 服务
        - id: smart-flow-core
          uri: lb://video_cso_exec_engine-flow-engine-core
          predicates:
            - Path=/video_cso_exec_engine/api/flow/core/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 500
                redis-rate-limiter.burstCapacity: 1000

        # 一般服务路由 → 转发到 video-cso-exec-engine-general 服务
        - id: smart-flow-general
          uri: lb://video_cso_exec_engine-flow-engine-general
          predicates:
            - Path=/video_cso_exec_engine/api/flow/general/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 200
                redis-rate-limiter.burstCapacity: 500
```

> **扩展方式**：新增服务级别（如 `realtime`）时，只需：  
> 1. 以 `SERVICE_TIER=realtime` 启动新实例（自动注册为 `smart-flow-engine-realtime`）  
> 2. 网关增加一条路由规则（`Path=/video_cso_exec_engine/api/flow/realtime/**`，`uri: lb://video_cso_exec_engine-flow-engine-realtime`）  
> 3. 在服务端 `VALID_TIERS` 配置中加入 `realtime`  
> 无需修改任何业务代码。

### 3.4.7 Feign 客户端（供网关调用参考）

网关侧通过 Nacos 发现对应服务级别的独立服务名，使用 Feign 调用。每个服务级别对应一个 FeignClient：

```java
@FeignClient(name = "video-cso-exec-engine-core", path = "/video_cso_exec_engine")
public interface SmartFlowCoreFeignClient {

    @PostMapping("/api/flow/{serviceTier}/{flowCode}")
    FlowApiResponse executeFlow(
        @PathVariable("serviceTier") String serviceTier,
        @PathVariable("flowCode") String flowCode,
        @RequestBody Map<String, Object> body
    );
}

@FeignClient(name = "video-cso-exec-engine-general", path = "/video_cso_exec_engine")
public interface SmartFlowGeneralFeignClient {

    @PostMapping("/api/flow/{serviceTier}/{flowCode}")
    FlowApiResponse executeFlow(
        @PathVariable("serviceTier") String serviceTier,
        @PathVariable("flowCode") String flowCode,
        @RequestBody Map<String, Object> body
    );
}
```

> 也可通过 `@FeignClient(name = "smart-flow-engine-${tier}")` 配合动态代理实现统一调用。

## 3.5 FlowGram Runtime 对接

### 3.5.1 执行服务封装

```java
@Service
public class FlowExecutionService {

    @Autowired
    private RestTemplate restTemplate;

    @Value("${flowgram.agents-server.base-url}")
    private String agentsServerBaseUrl;

    /**
     * 执行流程，通过 sync 字段控制同步/异步模式。
     * sync=true 时 agents-server 直接返回执行结果；
     * sync=false 时 agents-server 立即返回 taskId，调用方后续查询结果。
     */
    public FlowExecutionResult executeFlow(FlowRoute route, Map<String, Object> inputs,
                                            boolean sync, HttpServletRequest request) {
        String schemaJson = loadSchema(route.getPublishId());

        Map<String, Object> runBody = new HashMap<>();
        runBody.put("schema", schemaJson);
        runBody.put("inputs", inputs);
        runBody.put("sync", sync);

        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        headers.set("x-call-source", "gateway");
        headers.set("x-agent-id", String.valueOf(route.getAgentId()));
        headers.set("x-flow-code", route.getFlowCode());

        HttpEntity<Map<String, Object>> entity = new HttpEntity<>(runBody, headers);
        Map<String, Object> runResult = restTemplate.postForObject(
            agentsServerBaseUrl + "/api/task/run", entity, Map.class);

        if (sync) {
            return FlowExecutionResult.from(runResult);
        }

        String taskId = (String) runResult.get("taskID");
        return FlowExecutionResult.async(taskId);
    }
}
```

## 3.6 编辑器与运行时集成

| 运行时模式 | 适用场景 | 说明 |
|-----------|---------|------|
| browser | 仅 demo/纯前端演示 | 前端模拟执行，不落库 |
| server | 编辑器内测试运行 | 直连 agents-server |
| proxy | 生产环境代理 | 通过 Nginx/API 网关代理到 agents-server |

前端编辑器通过 `createRuntimePlugin(mode, serverConfig)` 配置运行时模式。开发环境通过 Rsbuild 代理 `/api/agents`、`/api/task/*` 到 agents-server。

---

# 4 对外调用方案

## 4.1 方案总览

本方案定义从「能力接入」到「能力开放」的完整链路，包含五个核心环节：

```
┌──────────┐     ┌──────────┐     ┌───────────────┐     ┌───────────────┐     ┌──────────────  ┐
│ 能力接入  │ ──→ │ 能力编排   │ ──→ │  能力发布      │ ──→ │ 动态路由注册    │ ──→ │  能力开放       │
│(原子API) │     │(可视化)    │     │(参数提取+API)  │     │(Nacos+路由表)  │     │(市场+网关)      │
└──────────┘     └──────────┘     └───────────────┘     └───────────────┘     └──────────────  ┘
```

## 4.2 流程发布

### 4.2.1 发布流程

```
编辑器「发布」按钮
    │                                               
    ▼
┌──────────────────────────────────────────────┐
│  Step 1: 参数自动提取                          │
│  - 解析 design_json 中 start 节点的 inputs     │
│  - 自动识别字段名、推断类型、提取默认值            │
│  - 标记必填字段（有值字段默认必填）               │
└──────────────────────┬──────────────────────┘
                       ▼
┌──────────────────────────────────────────────┐
│  Step 2: 发布信息填写                          │
│  - 能力名称、描述、分类标签                      │
│  - ★ serviceTier 服务级别（core/general/...）  │
│  - flowCode（路由标识，自动生成可修改）           │
│  - 版本号（默认 1.0.0）                         │
│  - 鉴权方式选择                                 │
│  - 参数元数据编辑（可调整类型、必填、描述）          │
│  - 响应结构体示例 + 说明                         │
└──────────────────────┬─────────────────────── ┘
                       ▼
┌──────────────────────────────────────────────  ┐
│  Step 3: 发布校验                                │
│  - 必填项检查                                    │
│  - flowCode 唯一性校验                           │
│  - Schema 合法性校验（调用 validate 接口）         │
│  - 试运行检查（可选）                             │
└──────────────────────┬───────────────────────  ┘
                       ▼
┌────────────────────────────────────────────── ┐
│  Step 4: 发布执行                              │
│  - 写入 flow_publish 表（发布记录）              │
│  - 写入 flow_api_params 表（参数定义）           │
│  - 写入 flow_route 表（路由注册）                │
│  - Schema JSON 加密存储（对外不可见）             │
│  - 清除路由缓存，使新路由立即生效                  │
│  - 返回发布结果（含调用地址）                      │
└────────────────────────────────────────────── ┘
```

### 4.2.2 参数自动提取算法

从 `design_json` 的 start 节点中提取 inputs，自动推断类型：

```java
public class InputParamExtractor {

    /**
     * 从 design_json 提取 start 节点的 inputs 参数定义
     * 
     * 输入示例:
     * inputs: {
     *   alarmTimeStart: "2021-3-2 23:59:59",
     *   pageNum: 1,
     *   alarmTimeEnd: "2026-3-10 23:59:59",
     *   pageSize: 6
     * }
     * 
     * 输出: 参数列表，含 name / type / required / defaultValue / description
     */
    public List<FlowParamDefinition> extractInputParams(String designJson) {
        JsonNode root = objectMapper.readTree(designJson);
        JsonNode startNode = findStartNode(root);
        JsonNode inputs = extractInputsFromStartNode(startNode);

        List<FlowParamDefinition> params = new ArrayList<>();
        Iterator<Map.Entry<String, JsonNode>> fields = inputs.fields();

        while (fields.hasNext()) {
            Map.Entry<String, JsonNode> field = fields.next();
            FlowParamDefinition param = new FlowParamDefinition();
            param.setParamName(field.getKey());
            param.setParamType(inferType(field.getValue()));
            param.setRequired(true);  // 默认必填
            param.setDefaultValue(field.getValue().isNull() ? null : field.getValue().asText());
            param.setDescription("");  // 待用户补充
            params.add(param);
        }
        return params;
    }

    private String inferType(JsonNode value) {
        if (value.isTextual()) return "String";
        if (value.isInt()) return "Integer";
        if (value.isLong()) return "Long";
        if (value.isDouble() || value.isFloat()) return "Double";
        if (value.isBoolean()) return "Boolean";
        if (value.isArray()) return "Array";
        if (value.isObject()) return "Object";
        return "String";  // 默认 String
    }
}
```

以用户示例为例，自动提取结果：

| 参数名 | 推断类型 | 必填 | 默认值 | 描述 |
|--------|---------|------|--------|------|
| alarmTimeStart | String | 是 | 2021-3-2 23:59:59 | （待填写） |
| pageNum | Integer | 是 | 1 | （待填写） |
| alarmTimeEnd | String | 是 | 2026-3-10 23:59:59 | （待填写） |
| pageSize | Integer | 是 | 6 | （待填写） |

### 4.2.3 发布 API 接口定义

```
POST /video_cso_exec_engine/api/publish
```

**请求体**：

```json
{
  "agentId": 5,
  "serviceTier": "core",
  "flowCode": "warning",
  "name": "告警查询能力",
  "description": "根据时间范围和分页参数查询告警信息",
  "version": "1.0.0",
  "category": "告警管理",
  "tags": ["告警", "查询"],
  "authModes": ["TOKEN", "ACCESS_KEY"],
  "params": [
    {
      "paramName": "alarmTimeStart",
      "paramType": "String",
      "paramIn": "body",
      "required": true,
      "defaultValue": "2021-3-2 23:59:59",
      "description": "告警开始时间，格式 yyyy-M-d HH:mm:ss"
    },
    {
      "paramName": "pageNum",
      "paramType": "Integer",
      "paramIn": "body",
      "required": true,
      "defaultValue": "1",
      "description": "页码，从1开始"
    },
    {
      "paramName": "alarmTimeEnd",
      "paramType": "String",
      "paramIn": "body",
      "required": true,
      "defaultValue": "2026-3-10 23:59:59",
      "description": "告警结束时间"
    },
    {
      "paramName": "pageSize",
      "paramType": "Integer",
      "paramIn": "body",
      "required": true,
      "defaultValue": "6",
      "description": "每页条数"
    }
  ],
  "responseExample": {
    "code": 200,
    "msg": "请求成功",
    "data": {
      "total": 100,
      "list": [
        {
          "processId": "f6dd80b0487b4f...",
          "cityName": "林芝市",
          "latitude": "29.637732",
          "orderStatus": "1"
        }
      ]
    }
  },
  "responseDescription": [
    { "field": "code", "type": "Integer", "description": "状态码，200表示成功" },
    { "field": "msg", "type": "String", "description": "响应消息" },
    { "field": "data.total", "type": "Integer", "description": "总记录数" },
    { "field": "data.list", "type": "Array", "description": "数据列表" },
    { "field": "data.list[].processId", "type": "String", "description": "流程ID" },
    { "field": "data.list[].cityName", "type": "String", "description": "城市名称" }
  ]
}
```

**响应体**：

```json
{
  "code": 200,
  "msg": "发布成功",
  "data": {
    "publishId": 1001,
    "serviceTier": "core",
    "flowCode": "warning",
    "routePath": "/video_cso_exec_engine/api/flow/core/warning",
    "fullUrl": "https://gateway.example.com/video_cso_exec_engine/api/flow/core/warning",
    "status": "ACTIVE",
    "version": "1.0.0",
    "publishTime": "2026-03-03T14:30:00+08:00"
  }
}
```

## 4.3 动态路由注册

### 4.3.1 路由注册流程

```
发布服务
    │                                                                                   
    ├─ 1. 写入 flow_publish 表（含 serviceTier + schema_json 加密存储）                 
    │                                                                                   
    ├─ 2. 写入 flow_route 表（serviceTier, flowCode, publishId, agentId, status=ACTIVE）
    │                                                                                   
    ├─ 3. 写入 flow_api_params 表（参数定义）                                           
    │                                                                                   
    ├─ 4. 清除 Redis 路由缓存                                                           
    │     └─ DEL smart:flow:route:{serviceTier}:{flowCode}                              
    │                                                                                   
    └─ 5. 路由立即生效                                                                  
          └─ 下次请求 /video_cso_exec_engine/api/flow/{serviceTier}/{flowCode} 时                       
             FlowDispatchController 查到 ACTIVE 路由即可调度
```

### 4.3.2 路由规则与约束

| 规则 | 说明 |
|------|------|
| **flowCode 唯一性** | 同一 serviceTier 下，同一时间只能有一个 ACTIVE 状态的同名 flowCode |
| **flowCode 命名规范** | 仅允许小写字母、数字、短横线，长度 3~64，如 `warning`、`device-query` |
| **serviceTier 取值** | 预定义枚举：`core`（核心）、`general`（一般），可扩展 `realtime`、`batch` 等 |
| **路由路径格式** | 固定前缀 `/video_cso_exec_engine/api/flow/` + serviceTier + `/` + flowCode |
| **HTTP 方法** | 默认 POST，统一使用 JSON Body 传参 |
| **路由状态流转** | DRAFT → ACTIVE → INACTIVE（下架） → ACTIVE（重新上架） |
| **并发安全** | 发布/下架操作需加分布式锁（Redis SETNX），防止并发冲突 |
| **优雅下线** | 下架时等待进行中的请求完成（最长等待 30s），再标记 INACTIVE |
| **级别变更** | 变更 serviceTier 等同于下架旧路由 + 发布新路由（URL 变化需通知调用方） |

### 4.3.3 路由状态机

```
          publish()             unpublish()           republish()
 DRAFT ───────────→ ACTIVE ──────────────→ INACTIVE ──────────────→ ACTIVE
   │                  │                       │
   │   delete()       │   update()            │   delete()
   ▼                  ▼                       ▼
 DELETED         新版本 ACTIVE              DELETED
                 旧版本 INACTIVE
```

## 4.4 API 接口定义规范

### 4.4.1 统一调用地址

| 入口 | 调用地址 | 鉴权方式 |
|------|---------|---------|
| **视联平台网关（核心）** | `https://slpt-gw.example.com/video_cso_exec_engine/api/flow/core/{flowCode}` | Token 鉴权 |
| **视联平台网关（一般）** | `https://slpt-gw.example.com/video_cso_exec_engine/api/flow/general/{flowCode}` | Token 鉴权 |
| **能力开放网关（核心）** | `https://open-gw.example.com/video_cso_exec_engine/api/flow/core/{flowCode}` | AccessKey 签名 |
| **能力开放网关（一般）** | `https://open-gw.example.com/video_cso_exec_engine/api/flow/general/{flowCode}` | AccessKey 签名 |
| **内网直连**（开发/测试） | OpenFeign调用 | 无鉴权 |

### 4.4.2 通用请求规范

#### 请求头（Headers）

| Header | 必填 | 说明 |
|--------|------|------|
| Content-Type | 是 | 固定 `application/json` |
| Authorization | 视联网关必填 | Bearer Token，视联网关统一认证签发 |
| X-Access-Key | 开放网关必填 | 能力开放平台分配的 AccessKey |
| X-Signature | 开放网关必填 | 请求签名（HMAC-SHA256） |
| X-Timestamp | 开放网关必填 | 请求时间戳（毫秒），有效期 ±5 分钟 |
| X-Nonce | 开放网关建议 | 随机字符串，防重放 |
| X-Request-Id | 否 | 调用方自定义请求追踪 ID |

#### 请求参数（Query）

通常不使用 Query 参数，所有业务参数统一放 Body。但保留扩展能力：

| Query 参数 | 必填 | 说明 |
|-----------|------|------|
| _version | 否 | 指定调用的流程版本号，默认调用最新 ACTIVE 版本 |
| _timeout | 否 | 自定义超时时间（秒），覆盖默认值 |

#### 请求体（Body）

Body 结构为 JSON 对象，包含系统控制字段和业务参数。`sync` 字段（布尔值，默认 `true`）控制同步/异步执行模式：
- `sync: true`（默认）：服务端等待工作流执行完成后返回结果
- `sync: false`：服务端立即返回 `taskId`，调用方后续通过结果查询接口获取结果

```json
{
  "sync": true,
  "alarmTimeStart": "2021-3-2 23:59:59",
  "pageNum": 1,
  "alarmTimeEnd": "2026-3-10 23:59:59",
  "pageSize": 6
}
```

### 4.4.3 统一响应规范

#### 同步模式响应

```json
{
  "code": 200,
  "msg": "请求成功",
  "requestId": "req-20260303-abcdef",
  "timestamp": 1741012345678,
  "data": {
    // 工作流 end 节点的 outputs
  }
}
```

#### 异步模式响应（`sync: false`）

```json
{
  "code": 200,
  "msg": "任务已提交",
  "requestId": "req-20260303-abcdef",
  "timestamp": 1741012345678,
  "data": {
    "taskId": "task-xxxx-xxxx",
    "statusUrl": "/video_cso_exec_engine/api/flow/core/warning/status?taskId=task-xxxx-xxxx",
    "resultUrl": "/video_cso_exec_engine/api/flow/core/warning/result?taskId=task-xxxx-xxxx"
  }
}
```

#### 异步结果查询

```
GET /video_cso_exec_engine/api/flow/{serviceTier}/{flowCode}/result?taskId={taskId}
```

```json
{
  "code": 200,
  "msg": "执行完成",
  "data": {
    "taskId": "task-xxxx-xxxx",
    "status": "success",
    "outputs": { ... },
    "startTime": "2026-03-03T14:30:00+08:00",
    "endTime": "2026-03-03T14:30:02+08:00",
    "durationMs": 2150
  }
}
```

#### 错误码定义

| code | msg | 说明 |
|------|-----|------|
| 200 | 请求成功 | 正常返回 |
| 400 | 参数校验失败 | 必填参数缺失或类型错误 |
| 401 | 认证失败 | Token/AccessKey 无效或签名错误 |
| 403 | 无权限 | 未订阅该能力或配额已用尽 |
| 404 | 能力不存在 | flowCode 不存在或已下架 |
| 408 | 执行超时 | 工作流执行超过超时时间 |
| 429 | 请求频率超限 | 超出调用频率限制 |
| 500 | 系统内部错误 | 服务端异常 |
| 502 | 上游服务异常 | agents-server 不可用 |

### 4.4.4 鉴权方案详解

#### AccessKey 签名认证（能力开放网关）

适用于第三方系统调用，安全级别更高：

**签名算法**：

```
1. 构造待签名字符串:
   StringToSign = HTTP-Method + "\n"
                + URI + "\n"
                + Timestamp + "\n"
                + Nonce + "\n"
                + SHA256(RequestBody)

2. 计算签名:
   Signature = Base64(HMAC-SHA256(AccessKeySecret, StringToSign))

3. 请求头:
   X-Access-Key: AK20260303xxxxx
   X-Signature: <Signature>
   X-Timestamp: 1741012345678
   X-Nonce: random-string-uuid
```

**调用示例（Java）**：

```java
public class OpenGatewayClient {

    private String accessKey = "AK20260303xxxxx";
    private String accessSecret = "your-secret-key";
    private String gatewayUrl = "https://open-gateway.example.com";

    public String callFlow(String serviceTier, String flowCode, String bodyJson) throws Exception {
        String uri = "/video_cso_exec_engine/api/flow/" + serviceTier + "/" + flowCode;
        String timestamp = String.valueOf(System.currentTimeMillis());
        String nonce = UUID.randomUUID().toString();
        String bodyHash = sha256Hex(bodyJson);

        String stringToSign = "POST\n" + uri + "\n" + timestamp + "\n" + nonce + "\n" + bodyHash;
        String signature = base64(hmacSha256(accessSecret, stringToSign));

        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create(gatewayUrl + uri))
            .header("Content-Type", "application/json")
            .header("X-Access-Key", accessKey)
            .header("X-Signature", signature)
            .header("X-Timestamp", timestamp)
            .header("X-Nonce", nonce)
            .POST(HttpRequest.BodyPublishers.ofString(bodyJson))
            .build();

        return httpClient.send(request, HttpResponse.BodyHandlers.ofString()).body();
    }
}
```

**调用示例（cURL）**：

```bash
curl -X POST "https://open-gateway.example.com/video_cso_exec_engine/api/flow/core/warning" \
  -H "Content-Type: application/json" \
  -H "X-Access-Key: AK20260303xxxxx" \
  -H "X-Signature: <computed-signature>" \
  -H "X-Timestamp: 1741012345678" \
  -H "X-Nonce: random-uuid" \
  -d '{
    "alarmTimeStart": "2021-3-2 23:59:59",
    "pageNum": 1,
    "alarmTimeEnd": "2026-3-10 23:59:59",
    "pageSize": 6
  }'
```

### 4.4.5 调用方使用流程

```
┌───────────────────────────────────────────────────────────────────────────── ┐
│                         调用方使用全流程                                        │
│                                                                              │    
│  1. 发现能力                                                                  │
│     └─ 进入「能力市场」页面 /marketplace                                        │
│     └─ 按分类/标签/关键词搜索所需的流程能力                                        │
│                                                                              │    
│  2. 查看文档                                                                  │
│     └─ 点击能力卡片进入详情页                                                   │
│     └─ 查看 API 地址、请求参数、响应结构、鉴权方式、使用示例                        │
│                                                                             │    
│  3. 获取凭证                                                                  │
│     └─ 视联平台：获取 Token                                                    │
│     └─ 开放网关：获取 AccessKey + AccessKeySecret                              │
│                                                                              │    
│  4. 在线调试                                                                  │
│     └─ 点击「去调试」按钮                                                       │
│     └─ 在调试工具中填写参数、自动携带凭证、发起请求                                  │
│     └─ 查看请求/响应结果                                                        │
│                                                                              │    
│  5. 正式对接                                                                  │
│     └─ 业务后台按 API 文档构造 HTTP 请求                                        │
│     └─ 通过网关调用 POST /video_cso_exec_engine/api/flow/{serviceTier}/{flowCode}
│     └─ 解析统一响应结构获取业务数据                                              │
│                                                                             │    
│  6. 运行监控                                                                  │
│     └─ 在能力市场「我的订阅」查看调用统计                                         │
│     └─ 查看调用量、成功率、平均耗时                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 4.5 能力详情页 API 文档展示规范

发布后自动生成的 API 文档页面应包含以下信息：

### 4.5.1 API 接口区

```
┌─────────────────────────────────────────────────────────     ┐
│  API 接口                                                     │
│                                                              │     
│  ┌───────────────────────────────────────────────────     ┐  │
│  │  告警查询能力 v1.0.0                                     │  │
│  └───────────────────────────────────────────────────     ┘  │
│                                                              │     
│  调用地址:  /video_cso_exec_engine/api/flow/core/warning                      │
│  请求方式:  POST                                              │
│  服务级别:  core（核心服务）                                    │
│  返回类型:  JSON                                              │
│                                                              │     
│  API调用:  API Token认证调用方法（视联网关） 收起 ▲               │
│            API签名认证调用方法（AccessKey）                     │
│                                                              │     
│  调试工具:  [去调试]                                           │
│                                                              │     
│  ┌─ 请求参数（Headers）────────────────────────────     ┐      │
│  │ 参数名          类型      必填   说明                 │      │
│  │ Content-Type   String    是    application/json     │     │
│  │ Authorization  String    是*   Bearer Token(视联网关)│     │
│  │ X-Access-Key   String    是*   密钥(开放网关)         │     │
│  └──────────────────────────────────────────────────   ┘    │
│                                                             │     
│  ┌─ 请求参数（Body）───────────────────────────────     ┐     │
│  │ 参数名           类型      必填  默认值               │     │
│  │                                  说明               │     │
│  │ alarmTimeStart  String    是    2021-3-2 23...      │     │
│  │                                  告警开始时间         │     │
│  │ pageNum         Integer   是    1                   │     │
│  │                                  页码               │     │
│  │ alarmTimeEnd    String    是    2026-3-10 23...     │     │
│  │                                  告警结束时间         │     │
│  │ pageSize        Integer   是    6                   │     │
│  │                                  每页条数            │     │
│  └──────────────────────────────────────────────────    ┘    │
│                                                         │     
│  ┌─ 返回内容 ──────────────────────────────────────     ┐     │
│  │ 字段              类型     说明                      │     │
│  │ code             Integer  状态码                    │     │
│  │ msg              String   响应消息                  │     │
│  │ data.total       Integer  总记录数                  │     │
│  │ data.list        Array    数据列表                  │     │
│  │ data.list[].processId  String  流程ID              │     │
│  │ data.list[].cityName   String  城市名称             │     │
│  └──────────────────────────────────────────────────  ┘    │
│                                                            │     
│  ▼ 使用示例                                                  │
└─────────────────────────────────────────────────────────    ┘
```

### 4.5.2 使用示例区

```json
// === 请求示例 ===
POST /video_cso_exec_engine/api/flow/core/warning
Content-Type: application/json
Authorization: Bearer <token>

{
  "alarmTimeStart": "2021-3-2 23:59:59",
  "pageNum": 1,
  "alarmTimeEnd": "2026-3-10 23:59:59",
  "pageSize": 6
}

// === 响应示例 ===
{
  "code": 200,
  "msg": "请求成功",
  "requestId": "req-20260303-f8a3b2c1",
  "timestamp": 1741012345678,
  "data": {
    "total": 156,
    "list": [
      {
        "processId": "f6dd80b0487b4f...",
        "relLinkType": "1",
        "flowEndTime": "...",
        "relDevice": {},
        "videoList": [],
        "cityName": "林芝市",
        "latitude": "29.637732",
        "orderStatus": "1",
        "warningLevel": "高"
      }
    ]
  }
}
```

---

# 5 数据库设计

## 5.1 核心表结构

> **信创兼容说明**：以下 DDL 基于 TaurusDB 8.0 语法。若部署信创数据库（达梦 DM8、人大金仓 KingbaseES 等），需按第 7 章适配矩阵进行 SQL 方言转换，主要差异包括：`AUTO_INCREMENT` → 序列/自增语法、`LONGTEXT` → `CLOB`/`TEXT`、`ENGINE=InnoDB` 去除、时间函数适配等。具体转换规则见 [7.3 数据库适配详细方案](#73-数据库适配详细方案关系型数据库)。

### 5.1.1 能力接入表 (capability)

```sql
CREATE TABLE capability (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    cap_code        VARCHAR(64)   NOT NULL COMMENT '能力编码（唯一标识）',
    name            VARCHAR(200)  NOT NULL COMMENT '能力名称',
    description     VARCHAR(2000) DEFAULT NULL COMMENT '能力描述',
    category_id     BIGINT        DEFAULT NULL COMMENT '分类ID',
    provider        VARCHAR(200)  DEFAULT NULL COMMENT '服务提供商',
    request_method  VARCHAR(10)   NOT NULL DEFAULT 'POST' COMMENT '请求方式: GET/POST/PUT/DELETE',
    request_url     VARCHAR(500)  NOT NULL COMMENT '调用地址',
    status          VARCHAR(20)   NOT NULL DEFAULT 'DRAFT' COMMENT '状态: DRAFT/ONLINE/OFFLINE',
    created_by      VARCHAR(100)  DEFAULT NULL COMMENT '创建人',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_cap_code (cap_code),
    INDEX idx_category (category_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='能力接入表';
```

### 5.1.2 能力文档表 (capability_doc)

```sql
CREATE TABLE capability_doc (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    capability_id   BIGINT        NOT NULL COMMENT '关联能力ID',
    version_id      BIGINT        DEFAULT NULL COMMENT '关联版本ID（NULL表示默认文档）',
    doc_type        VARCHAR(20)   NOT NULL COMMENT '文档类型: HEADER/QUERY/BODY/RESPONSE/ERROR_CODE',
    content         LONGTEXT      NOT NULL COMMENT '文档内容 JSON（参数列表/结构体定义）',
    example         LONGTEXT      DEFAULT NULL COMMENT '示例 JSON',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_capability (capability_id),
    INDEX idx_version (version_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='能力文档表';
```

### 5.1.3 能力版本表 (capability_version)

```sql
CREATE TABLE capability_version (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    capability_id   BIGINT        NOT NULL COMMENT '关联能力ID',
    version_no      VARCHAR(20)   NOT NULL COMMENT '版本号（如 1.0.0）',
    domain          VARCHAR(32)   NOT NULL DEFAULT 'center' COMMENT '域标识: center(中心)/local(属地)',
    request_url     VARCHAR(500)  DEFAULT NULL COMMENT '该版本的调用地址（可覆盖主表）',
    status          VARCHAR(20)   NOT NULL DEFAULT 'DRAFT' COMMENT '状态: DRAFT/PUBLISHED/DEPRECATED',
    change_log      VARCHAR(2000) DEFAULT NULL COMMENT '版本变更说明',
    published_at    DATETIME      DEFAULT NULL COMMENT '发布时间',
    created_by      VARCHAR(100)  DEFAULT NULL COMMENT '创建人',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_cap_version_domain (capability_id, version_no, domain),
    INDEX idx_capability (capability_id),
    INDEX idx_domain (domain),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='能力版本表';
```

### 5.1.4 能力分类表 (capability_category)

```sql
CREATE TABLE capability_category (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    parent_id       BIGINT        DEFAULT NULL COMMENT '父分类ID（NULL为顶级）',
    name            VARCHAR(100)  NOT NULL COMMENT '分类名称',
    sort_order      INT           NOT NULL DEFAULT 0 COMMENT '排序序号',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_parent (parent_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='能力分类表';
```

### 5.1.5 智能体表 (agents)

```sql
CREATE TABLE agents (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    name            VARCHAR(200)  NOT NULL COMMENT '智能体名称',
    description     VARCHAR(1000) DEFAULT NULL COMMENT '描述',
    design_json     LONGTEXT      DEFAULT NULL COMMENT '工作流设计 JSON（FlowGram Schema）',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='智能体表';
```

### 5.1.6 任务执行记录表 (task_executions)

```sql
CREATE TABLE task_executions (
    id                  BIGINT AUTO_INCREMENT PRIMARY KEY,
    task_id             VARCHAR(64)   NOT NULL COMMENT '任务ID（FlowGram 生成）',
    agent_id            BIGINT        DEFAULT NULL COMMENT '关联智能体ID',
    service_tier        VARCHAR(32)   DEFAULT NULL COMMENT '服务级别标签: core/general',
    flow_code           VARCHAR(64)   DEFAULT NULL COMMENT '关联发布流程编码',
    source              VARCHAR(20)   NOT NULL DEFAULT 'api' COMMENT '来源: page/api/gateway',
    status              VARCHAR(20)   NOT NULL DEFAULT 'running' COMMENT '状态: running/success/failed/cancelled',
    input_params        LONGTEXT      DEFAULT NULL COMMENT '输入参数 JSON',
    output_result       LONGTEXT      DEFAULT NULL COMMENT '输出结果 JSON',
    error_message       TEXT          DEFAULT NULL COMMENT '错误信息',
    execution_log       LONGTEXT      DEFAULT NULL COMMENT '执行日志 JSON',
    node_logs           LONGTEXT      DEFAULT NULL COMMENT '节点日志 JSON',
    start_time          DATETIME      DEFAULT NULL,
    end_time            DATETIME      DEFAULT NULL,
    total_duration_ms   BIGINT        DEFAULT NULL COMMENT '总耗时(毫秒)',
    caller_ip           VARCHAR(45)   DEFAULT NULL COMMENT '调用方IP',
    request_id          VARCHAR(64)   DEFAULT NULL COMMENT '请求追踪ID',
    created_at          DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_task_id (task_id),
    INDEX idx_agent_id (agent_id),
    INDEX idx_flow_code (flow_code),
    INDEX idx_status (status),
    INDEX idx_start_time (start_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='任务执行记录表';
```

### 5.1.7 流程发布表 (flow_publish)

```sql
CREATE TABLE flow_publish (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    agent_id        BIGINT        NOT NULL COMMENT '关联智能体ID',
    service_tier    VARCHAR(32)   NOT NULL DEFAULT 'general' COMMENT '服务级别标签: core/general/自定义',
    flow_code       VARCHAR(64)   NOT NULL COMMENT '流程编码（路由标识）',
    name            VARCHAR(200)  NOT NULL COMMENT '能力名称',
    description     VARCHAR(2000) DEFAULT NULL COMMENT '能力描述',
    version         VARCHAR(20)   NOT NULL DEFAULT '1.0.0' COMMENT '版本号',
    category        VARCHAR(100)  DEFAULT NULL COMMENT '分类',
    tags            VARCHAR(500)  DEFAULT NULL COMMENT '标签，逗号分隔',
    schema_json     LONGTEXT      NOT NULL COMMENT '工作流 Schema JSON（加密存储，对外不可见）',
    auth_modes      VARCHAR(100)  NOT NULL DEFAULT 'TOKEN' COMMENT '支持的鉴权方式，逗号分隔',
    status          VARCHAR(20)   NOT NULL DEFAULT 'ACTIVE' COMMENT '状态: DRAFT/ACTIVE/INACTIVE/DELETED',
    response_example     LONGTEXT DEFAULT NULL COMMENT '响应示例 JSON',
    response_description LONGTEXT DEFAULT NULL COMMENT '响应字段说明 JSON',
    published_by    VARCHAR(100)  DEFAULT NULL COMMENT '发布人',
    published_at    DATETIME      DEFAULT NULL COMMENT '发布时间',
    unpublished_at  DATETIME      DEFAULT NULL COMMENT '下架时间',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_tier_code_version (service_tier, flow_code, version),
    INDEX idx_agent_id (agent_id),
    INDEX idx_status (status),
    INDEX idx_category (category),
    INDEX idx_service_tier (service_tier)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='流程发布表';
```

### 5.1.8 流程 API 参数表 (flow_api_params)

```sql
CREATE TABLE flow_api_params (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    publish_id      BIGINT        NOT NULL COMMENT '关联发布ID',
    param_name      VARCHAR(100)  NOT NULL COMMENT '参数名',
    param_type      VARCHAR(20)   NOT NULL DEFAULT 'String' COMMENT '参数类型: String/Integer/Long/Double/Boolean/Object/Array',
    param_in        VARCHAR(20)   NOT NULL DEFAULT 'body' COMMENT '参数位置: header/query/body',
    required        TINYINT(1)    NOT NULL DEFAULT 1 COMMENT '是否必填: 0-否 1-是',
    default_value   VARCHAR(500)  DEFAULT NULL COMMENT '默认值',
    description     VARCHAR(1000) DEFAULT NULL COMMENT '参数说明',
    example_value   VARCHAR(500)  DEFAULT NULL COMMENT '示例值',
    sort_order      INT           NOT NULL DEFAULT 0 COMMENT '排序序号',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_publish_id (publish_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='流程API参数定义表';
```

### 5.1.9 流程路由表 (flow_route)

```sql
CREATE TABLE flow_route (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    service_tier    VARCHAR(32)   NOT NULL DEFAULT 'general' COMMENT '服务级别标签: core/general/自定义',
    flow_code       VARCHAR(64)   NOT NULL COMMENT '流程编码',
    publish_id      BIGINT        NOT NULL COMMENT '关联发布记录ID',
    agent_id        BIGINT        NOT NULL COMMENT '关联智能体ID',
    route_path      VARCHAR(200)  NOT NULL COMMENT '完整路由路径 /video_cso_exec_engine/api/flow/{tier}/{code}',
    http_method     VARCHAR(10)   NOT NULL DEFAULT 'POST' COMMENT 'HTTP方法',
    status          VARCHAR(20)   NOT NULL DEFAULT 'ACTIVE' COMMENT '路由状态: ACTIVE/INACTIVE',
    timeout_seconds INT           NOT NULL DEFAULT 120 COMMENT '超时时间(秒)',
    rate_limit_qps  INT           DEFAULT NULL COMMENT '限流 QPS',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_tier_code_active (service_tier, flow_code, status),
    INDEX idx_publish_id (publish_id),
    INDEX idx_service_tier (service_tier)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='流程路由表';
```

### 5.1.10 能力收藏表 (flow_favorite)

```sql
CREATE TABLE flow_favorite (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
    flow_code       VARCHAR(64)   NOT NULL COMMENT '流程/能力编码',
    user_id         VARCHAR(100)  NOT NULL COMMENT '收藏用户ID（替代原订阅方标识）',
    user_name       VARCHAR(200)  DEFAULT NULL COMMENT '收藏用户名称',
    favorite_name   VARCHAR(200)  DEFAULT NULL COMMENT '收藏自定义名称（方便用户识别）',
    sort_weight     INT           DEFAULT 0 COMMENT '排序权重（数值越大越靠前）',
    is_starred      TINYINT       DEFAULT 0 COMMENT '是否星标（0-否 1-是）',
    status          VARCHAR(20)   NOT NULL DEFAULT 'ACTIVE' COMMENT '收藏状态: ACTIVE(有效)/DELETED(已删除)',
    remark          VARCHAR(500)  DEFAULT NULL COMMENT '收藏备注（用户自定义）',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '收藏时间',
    updated_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    -- 唯一索引：一个用户只能收藏同一个能力一次
    UNIQUE INDEX idx_flow_user (flow_code, user_id),
    -- 普通索引：按用户/状态/星标快速查询
    INDEX idx_user_status (user_id, status),
    INDEX idx_user_starred (user_id, is_starred)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='能力收藏表';
```

### 5.1.11 调用统计表 (flow_call_stats)

```sql
CREATE TABLE flow_call_stats (
    id              BIGINT AUTO_INCREMENT PRIMARY KEY,
    flow_code       VARCHAR(64)   NOT NULL COMMENT '流程编码',
    stat_date       DATE          NOT NULL COMMENT '统计日期',
    total_calls     BIGINT        NOT NULL DEFAULT 0 COMMENT '总调用次数',
    success_calls   BIGINT        NOT NULL DEFAULT 0 COMMENT '成功次数',
    failed_calls    BIGINT        NOT NULL DEFAULT 0 COMMENT '失败次数',
    avg_duration_ms BIGINT        DEFAULT NULL COMMENT '平均耗时(毫秒)',
    max_duration_ms BIGINT        DEFAULT NULL COMMENT '最大耗时(毫秒)',
    p99_duration_ms BIGINT        DEFAULT NULL COMMENT 'P99耗时(毫秒)',
    created_at      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE INDEX idx_flow_date (flow_code, stat_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='调用统计表（按日聚合）';
```

## 5.2 ER 关系

```
capability_category (1) ──── (N) capability (通过 category_id 关联)

capability (1) ──────────── (N) capability_doc (通过 capability_id 关联)
capability (1) ──────────── (N) capability_version (通过 capability_id 关联)

capability_version (1) ──── (N) capability_doc (通过 version_id 关联，可选)

agents (1) ──────── (N) flow_publish
                          │
                          ├── (N) flow_api_params
                          │
                          └── (1) flow_route
                                    │
                                    └── (N) flow_subscription
                                    └── (N) flow_call_stats

agents (1) ──────── (N) task_executions
flow_route (1) ──── (N) task_executions (通过 flow_code 关联)
```

---

# 6 部署与运维

## 6.1 部署架构总体设计

### 6.1.1 设计原则

| 原则 | 说明 |
|------|------|
| **分级部署** | 根据业务重要程度区分核心集群与一般集群，资源配比差异化 |
| **水平可扩展** | 所有服务实例均为无状态设计，支持按需水平扩缩容 |
| **高可用优先** | 核心集群采用多副本 + 跨可用区部署，消除单点故障 |
| **弹性伸缩** | 结合华为云 CCE HPA / 手动扩容，适配业务并发峰谷变化 |
| **故障隔离** | 核心与一般业务物理隔离（独立 Namespace / Node Pool），避免资源争抢 |

### 6.1.2 集群分级定义

| 维度 | 核心业务集群（P0 高可靠） | 一般业务集群（P1 标准） |
|------|------------------------|----------------------|
| **定位** | 承载生产关键流程（告警处理、设备管控、安全合规等） | 承载内部工具、报表查询、非实时批处理等 |
| **可用性目标** | ≥ 99.95%（年停机 ≤ 4.4h） | ≥ 99.9%（年停机 ≤ 8.8h） |
| **部署模式** | 多副本 + 跨可用区 + 主备 DB + Redis Cluster | 多副本 + 单可用区 + DB 主从 + Redis Sentinel |
| **服务最小副本** | video-cso-exec-engine-core ≥ 3，agents-server ≥ 3 | video-cso-exec-engine-general ≥ 2，agents-server ≥ 2 |
| **弹性上限** | HPA max=20（按 CPU/QPS 自动扩） | HPA max=10 |
| **数据库** | 独立 TaurusDB 主从 + 读写分离 | 可共享 DB 实例（独立 Schema） |
| **缓存** | 独立 Redis Cluster（≥ 6 节点） | 共享 Redis Sentinel（3 节点） |
| **网络隔离** | 独立 Namespace + NetworkPolicy | 独立 Namespace |
| **发布策略** | 金丝雀发布 / 蓝绿部署 | 滚动更新 |
| **监控告警** | 1 分钟粒度 + 即时告警 + 值班 On-Call | 5 分钟粒度 + 告警通知 |

### 6.1.3 整体部署架构图

```
                            ┌──────────────────────────────────                   ┐
                            │          流量接入层                                   │
                            │  Nginx / SLB / Ingress Controller                   │
                            │  (SSL 终结, 域名路由, 静态资源)                        │
                            └───────────────┬──────────────────                   ┘
                                            │                                      
                       ┌────────────────────┼────────────────────                 ┐
                       │                    │                    │                 
                       ▼                    ▼                    ▼
              ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────        ┐
              │  视联平台网关     │  │  能力开放网关     │  │  内部管理入口             │
              │  (Token鉴权)    │  │  (AccessKey)     │  │  (编辑器/管理页)          │
              └────────┬────────┘  └────────┬────────┘  └────────┬────────        ┘
                       │                    │                    │                 
                       └───── Nacos 服务发现 ┼────────────────────                 ┘
                                            │                                      
          ┌─────────────────────────────────┼──────────────────────────────────   ┐
          │                                 │                                     │   
          ▼                                 ▼                                     │
┌═══════════════════════════════╗  ┌───────────────────────────────   ┐           │
║   核心业务集群 (P0 高可靠)    ║  │   一般业务集群 (P1 标准)               │           │
║   Namespace: smart-core       ║  │   Namespace: smart-general       │           │
║                               ║  │                                  │           │   
║  ┌─────────────────────────┐  ║  │  ┌─────────────────────────   ┐  │           │
║  │    video-cso-mgr        │  ║  │  │ video-cso-mgr              │  │           │
║  │ ┌────┐┌────┐┌────┐     │  ║  │  │ ┌────┐┌────     ┐           │  │           │
║  │ │ R1 ││ R2 ││ R3 │ ... │  ║  │  │ │ R1 ││ R2 │ ...            │  │           │
║  │ └────┘└────┘└────┘     │  ║  │  │ └────┘└────     ┘           │  │           │
║  │ min=2  max=3 (HPA)     │  ║  │  │ min=2  max=3 (HPA)          │  │           │
║  └────────────┬────────────┘  ║  │  └────────────┬────────────   ┘  │           │
║               │               ║  │               │                  │           │   
║  ┌────────────▼────────────┐  ║  │  ┌────────────▼────────────   ┐  │           │
║  │ video-cso-exec-engine-core ║  │  │video-cso-exec-engine-general  │           │
║  │ ┌────┐┌────┐┌────┐      │  ║  │  │ ┌────┐┌────     ┐           │  │          │
║  │ │ R1 ││ R2 ││ R3 │ ...  │  ║  │  │ │ R1 ││ R2 │ ...            │  │          │
║  │ └────┘└────┘└────┘      │  ║  │  │ └────┘└────     ┘           │  │          │
║  │ min=3  max=20 (HPA)     │  ║  │  │ min=2  max=10 (HPA)         │  │          │
║  └─────────────────────────┘  ║  │  └─────────────────────────   ┘  │           │
║                               ║  │                                  │           │   
║  ┌─────────────────────────┐  ║  │  ┌─────────────────────────   ┐  │           │
║  │  TaurusDB (独立主从)     │  ║  │  │  TaurusDB (共享实例)         │  │           │
║  │  Master ─→ Slave1/Slave2│  ║  │  │  独立 Schema                │  │           │
║  │  跨可用区                │  ║  │  │  Master ─→ Slave            │  │           │
║  └─────────────────────────┘  ║  │  └─────────────────────────   ┘  │           │
║                               ║  │                                  │           │   
║  ┌─────────────────────────┐  ║  │  ┌─────────────────────────   ┐  │           │
║  │  Redis Cluster (独立)    │  ║  │  │  Redis Sentinel (共享)     │  │           │
║  │  ≥ 4 节点 (2主2从)        │  ║  │  │  3 节点 (1主2从)            │  │           │
║  └─────────────────────────┘  ║  │  └─────────────────────────   ┘  │           │
║                               ║  │                                  │           │   
╚═══════════════════════════════╝  └───────────────────────────────   ┘           │
                                                                                  │   
┌──────────────────────────── 共享基础设施 ────────────────────────────────────     ┘
│                                                                                  
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────────────    ┐
│  │  Nacos 2.x       │  │  前端 (Nginx)     │  │  监控 & 日志                      │
│  │  集群 (3节点)     │  │  静态资源 + 代理   │  │  Prometheus + Grafana             │
│  │  注册中心 + 配置   │  │  CDN 加速          │  │  ELK / Loki 日志采集              │
│  └──────────────────┘  └──────────────────┘  └──────────────────────────────    ┘
```

### 6.1.4 基于 URL 路径的流量路由与集群隔离

流程发布时选择 `serviceTier`（core / general），对应 URL 路径中携带该标签。每个服务级别以独立的 Nacos 服务名注册，网关根据 **请求路径前缀** 将流量直接路由到对应的独立服务，实现物理级别的资源隔离：

```
调用方请求                           网关路由规则                        Nacos 独立服务
──────────────                     ──────────────                     ──────────────
POST /video_cso_exec_engine/api/flow/               路径匹配 /core/**                  video-cso-exec-engine-core
     core/warning          ──→      lb://video_cso_exec_engine-flow-engine-core  ──→   (Instance 1,2,3...)

POST /video_cso_exec_engine/api/flow/               路径匹配 /general/**               video-cso-exec-engine-general
     general/report-export ──→      lb://video_cso_exec_engine-flow-engine-general──→   (Instance 1,2...)
```

**Nacos 注册全景**：

```
┌──────────────────────────────────────────────────────────────────────    ┐
│  Nacos 服务注册（独立服务名模型）                                            │
│                                                                          │    
│  ┌── video-cso-mgr ─────────────────────────────────────      ┐          │
│  │  Instance-1  meta: {version:1.0.0, zone:az-1}                       │ │
│  │  Instance-2  meta: {version:1.0.0, zone:az-2}                       │ │
│  │  Instance-3  meta: {version:1.0.0, zone:az-1}                       │ │
│  │  ...         (HPA 动态扩展 max=20)                                    │ │
│  └──────────────────────────────────────────────────────────────       ┘ │
│                                                                          │
│  ┌── video-cso-exec-engine-core ──────────────────────────────────  ┐ │
│  │  Instance-1  meta: {version:1.0.0, zone:az-1}                       │ │
│  │  Instance-2  meta: {version:1.0.0, zone:az-1}                       │ │
│  │  ...         (HPA 动态扩展 max=10)                                   │ │
│  └──────────────────────────────────────────────────────────────       ┘ │
│                                                                          │
│  ┌── video-cso-exec-engine-general ──────────────────────────────────  ┐ │
│  │  Instance-1  meta: {version:1.0.0, zone:az-1}                       │ │
│  │  Instance-2  meta: {version:1.0.0, zone:az-1}                       │ │
│  │  ...         (HPA 动态扩展 max=10)                                   │ │
│  └──────────────────────────────────────────────────────────────       ┘ │
│                                                                          │    
│  ┌── video-cso-exec-engine-realtime (未来扩展) ──────────────────────   ┐ │
│  │  以新服务名部署实例 + 新增网关路由规则即可，无需改代码                      │ │
│  └──────────────────────────────────────────────────────────────       ┘ │
└──────────────────────────────────────────────────────────────────────    ┘
```

**部署启动命令**：

```bash
# 核心集群实例 — 注册为 video-cso-exec-engine-core，资源配置更高
SERVICE_TIER=core ZONE=az-1 java -Xms2g -Xmx4g -jar smart-flow-engine.jar

# 一般集群实例 — 注册为 video-cso-exec-engine-general，标准资源
SERVICE_TIER=general ZONE=az-1 java -Xms1g -Xmx2g -jar smart-flow-engine.jar
```

> **设计优势**：  
> 1. 同一套代码，通过环境变量 `SERVICE_TIER` 拼接为独立服务名，网关直接按服务名路由，配置简洁  
> 2. 无需依赖 Nacos metadata 过滤，路由逻辑更直观可靠  
> 3. 新增服务级别只需以新服务名部署实例 + 网关增加路由，零代码变更  
> 4. flowCode 在不同 tier 下可重名（如 core/warning 和 general/warning 互不影响）

## 6.2 水平扩展方案

### 6.2.1 服务无状态设计保障

所有服务实例严格无状态，扩缩容不影响业务连续性：

| 服务 | 无状态保障措施 | 说明 |
|------|-------------|------|
| smart-flow-engine-{tier} | Session 无状态（Token 鉴权）、路由数据存 DB/Redis | 新实例启动即可承载流量 |
| agents-server | 任务状态存 DB、报告缓存设 TTL 自动过期 | 不依赖本地持久化 |
| 前端 Nginx | 纯静态资源，无会话状态 | CDN + 多实例 |

### 6.2.2 华为云 CCE (Cloud Container Engine) HPA 弹性伸缩策略

> 以下 HPA 配置部署于华为云 CCE (Cloud Container Engine) 集群。

```yaml
# video-cso-exec-engine-core HPA — 核心集群
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: video-cso-exec-engine-core-hpa
  namespace: smart-core
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: video-cso-exec-engine-core
  minReplicas: 3
  maxReplicas: 20
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 30      # 快速扩容
      policies:
        - type: Pods
          value: 4                         # 每次最多扩 4 个 Pod
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300     # 缓慢缩容，避免抖动
      policies:
        - type: Pods
          value: 2
          periodSeconds: 120
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60          # CPU 超 60% 触发扩容
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 70          # 内存超 70% 触发扩容
    - type: Pods
      pods:
        metric:
          name: http_requests_per_second
        target:
          type: AverageValue
          averageValue: "200"             # 单 Pod QPS 超 200 触发扩容
---
# video-cso-exec-engine-general HPA — 一般集群
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: video-cso-exec-engine-general-hpa
  namespace: smart-general
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: video-cso-exec-engine-general
  minReplicas: 2
  maxReplicas: 10
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 600
      policies:
        - type: Pods
          value: 1
          periodSeconds: 180
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
```

### 6.2.3 agents-server（Node.js）水平扩展

agents-server 基于 Node.js 单线程模型，水平扩展尤为关键：

```
                    CCE Service / SLB
                         │                                        
          ┌──────────────┼──────────────                         ┐
          │              │              │                         
          ▼              ▼              ▼
   ┌─────────────┐┌─────────────┐┌─────────────                  ┐
   │ agents-svr-1││ agents-svr-2││ agents-svr-3│  ← Pod 级水平扩展
   │ :4000       ││ :4000       ││ :4000                         │
   │             ││             ││             │                  
   │ Node.js     ││ Node.js     ││ Node.js                       │
   │ 单进程      ││ 单进程      ││ 单进程                           │
   └─────────────┘└─────────────┘└─────────────                  ┘
         │              │              │                          
         └──────────────┼──────────────                          ┘
                        │                                         
              ┌─────────▼─────────                               ┐
              │  TaurusDB / Redis                                   │
              │  (共享状态存储)                                    │
              └───────────────────                               ┘
```

| 配置项 | 核心集群 | 一般集群 |
|--------|---------|---------|
| Pod 资源 requests | CPU: 500m, Memory: 512Mi | CPU: 250m, Memory: 256Mi |
| Pod 资源 limits | CPU: 2000m, Memory: 2Gi | CPU: 1000m, Memory: 1Gi |
| minReplicas | 3 | 2 |
| maxReplicas | 20 | 10 |
| HPA 触发阈值（CPU） | 60% | 70% |
| readinessProbe | `GET /health` 5s 间隔 | `GET /health` 10s 间隔 |
| 优雅关闭 | terminationGracePeriodSeconds: 60 | terminationGracePeriodSeconds: 30 |

### 6.2.4 容量规划参考

根据单实例基准压测数据，给出不同并发场景的实例数参考：

| 并发场景 | 预估 QPS | smart-flow-engine-{tier} 实例数 | agents-server 实例数 | TaurusDB 配置 | Redis 配置 |
|---------|---------|------------------------|---------------------|-----------|-----------|
| **小规模**（内部试用） | ≤ 50 | 2 | 2 | 单机 4C8G | 单机 |
| **中规模**（部门级） | 50~200 | 3~5 | 3~5 | 主从 8C16G | Sentinel 3 节点 |
| **大规模**（平台级） | 200~1000 | 5~12 | 5~12 | 主从+读写分离 16C32G | Cluster 6 节点 |
| **超大规模**（全网级） | 1000+ | 12~20+ | 12~20+ | 分库分表 / 云 RDS | Cluster 12+ 节点 |

> **单实例基准**（参考值，实际需压测验证）：  
> - smart-flow-engine-{tier}：单 Pod（2C4G）约承载 150~250 QPS（含 DB 查询 + 转发执行）  
> - agents-server：单 Pod（2C2G）约承载 80~150 并发工作流执行（受工作流复杂度影响大）

### 6.2.5 扩展触发与决策流程

```
监控采集 (Prometheus / Metrics Server)
    │                                                                         
    ▼
┌─────────────────────────────────────────────────────────────────────────── ┐
│  判断维度               │  核心集群阈值          │  一般集群阈值                │
├────────────────────────┼───────────────────────┼────────────────────────   ┤
│  Pod 平均 CPU           │  > 60% → 扩容         │  > 70% → 扩容              │
│  Pod 平均 Memory        │  > 70% → 扩容         │  > 80% → 扩容              │
│  单 Pod QPS             │  > 200 → 扩容         │  > 300 → 扩容             │
│  API P99 延迟           │  > 3s → 扩容+告警     │  > 5s → 扩容+告警           │
│  Pod 平均 CPU           │  < 20% 持续10min → 缩 │  < 20% 持续20min → 缩       │
└─────────────────────────┴───────────────────────┴────────────────────────  ┘
    │                                                                         
    ▼
┌──────────────────────────────────────────────────                         ┐
│  自动执行                                                                  │
│  ├─ CCE HPA 自动调整 Pod 副本数                                             │
│  ├─ 新 Pod 启动 → Nacos 自动注册 → 承载流量                                  │
│  ├─ 缩容 Pod → Nacos 自动注销 → 优雅关闭                                     │
│  └─ 超过 HPA max → 告警人工介入（扩 Node Pool）                              │
└──────────────────────────────────────────────────                         ┘
```

## 6.3 信创环境部署架构（定开场景）

信创环境同样遵循分级部署原则，替换底层组件：

```
┌──────────────── 银河麒麟 V10 / 统信 UOS / 鲲鹏 ARM64 ────────────────────  ┐
│                                                                            │
│  ┌── 核心业务集群 ──────────────────────────────────────────────────────┐    │
│  │                                                                      │  │
│  │  video-cso-exec-engine-core (Java)     agents-server (Node.js)           │  │
│  │  东方通 TongWeb / Tomcat           min=3, HPA max=20                  │  │
│  │  毕昇 JDK / 鲲鹏 JDK              Node.js ARM64 LTS                   │  │
│  │  min=3, HPA max=20                                                   │  │
│  │                                                                      │  │
│  │  达梦 DM8 / 金仓 (独立主从)        东方通 TongRDS (独立 Cluster)         │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                            │
│  ┌── 一般业务集群 ──────────────────────────────────────────────────────┐    │
│  │                                                                      │  │
│  │  video-cso-exec-engine-general (Java)  agents-server (Node.js)           │  │
│  │  min=2, HPA max=10                 min=2, HPA max=10                 │  │
│  │                                                                      │  │
│  │  达梦 DM8 / 金仓 (共享Schema)     东方通 TongRDS (共享 Sentinel)         │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                            │
│  ┌── 共享基础设施 ──────────────────────────────────────────────────────┐    │
│  │  Nacos 2.x (3节点)    Nginx     监控 (Prometheus + Grafana)           │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────┘
```

## 6.4 关键运维指标

| 指标 | 核心集群阈值 | 一般集群阈值 | 处理方式 |
|------|------------|------------|---------|
| API 响应时间 P99 | > 3s | > 5s | 告警 → HPA 扩容 → 检查 agents-server 负载 |
| 调用失败率 | > 1% | > 5% | 告警 → 检查上游服务可用性 → 熔断降级 |
| Pod CPU 使用率 | > 60% | > 70% | HPA 自动扩容 |
| Pod Memory 使用率 | > 70% | > 80% | HPA 自动扩容 / 检查内存泄漏 |
| agents-server 内存 | > 70% | > 80% | 触发 cleanup → HPA 扩容 |
| TaurusDB 慢查询 | > 500ms | > 1s | 优化索引或 SQL |
| Redis 命中率 | < 95% | < 90% | 检查缓存策略 |
| DB 连接池使用率 | > 80% | > 80% | 扩大连接池 / 读写分离 |
| Nacos 心跳超时 | 任何实例 | 任何实例 | 检查网络 / 重启实例 |

## 6.5 健康检查

| 服务 | 端点 | 检查频率（核心/一般） | 说明 |
|------|------|---------------------|------|
| smart-flow-engine-{tier} | `GET /video_cso_exec_engine/actuator/health` | 5s / 10s | Spring Boot Actuator，含 DB/Redis 探针 |
| agents-server | `GET /health` | 5s / 10s | Express 健康检查 |
| 前端 | `GET /agent/` | 30s | Nginx 静态资源可用性 |

CCE 探针配置：

```yaml
# 核心集群探针配置（更严格）
livenessProbe:
  httpGet:
    path: /video_cso_exec_engine/actuator/health/liveness
    port: 8080
  initialDelaySeconds: 30
  periodSeconds: 10
  failureThreshold: 3
readinessProbe:
  httpGet:
    path: /video_cso_exec_engine/actuator/health/readiness
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 5
  failureThreshold: 2
startupProbe:
  httpGet:
    path: /video_cso_exec_engine/actuator/health
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 5
  failureThreshold: 30
```

## 6.6 日志规范

| 层次 | 日志内容 | 存储 | 保留策略 |
|------|---------|------|---------|
| 网关层 | 请求 URL、鉴权结果、调用方标识、耗时 | 网关日志系统 | 90 天 |
| 路由层 | flowCode、参数校验结果、路由匹配、服务级别 | smart-flow-engine-{tier} 日志 → ELK/Loki | 30 天 |
| 执行层 | taskId、节点执行状态、输入输出 | task_executions 表 + 应用日志 | DB 永久，日志 30 天 |
| 基础设施 | Pod 生命周期、HPA 扩缩事件、健康检查失败 | CCE Events + Prometheus | 15 天 |

## 6.7 发布与变更管理

| 维度 | 核心集群 | 一般集群 |
|------|---------|---------|
| **发布策略** | 金丝雀发布（先 1 Pod → 观察 5min → 全量） | 滚动更新（maxSurge=1, maxUnavailable=0） |
| **回滚机制** | 自动回滚（健康检查失败 2 次触发） | 手动回滚 |
| **变更窗口** | 业务低峰期（如凌晨 2:00~5:00） | 工作日白天可变更 |
| **灰度验证** | 金丝雀流量占比 5% → 20% → 50% → 100% | 无灰度 |
| **变更审批** | 需审批 + 变更日志 | 需通知 |

---

# 附录

## 附录 A：完整调用时序图

```
调用方           视联平台网关         video-cso-exec-engine-core   agents-server       TaurusDB/Redis
  │                  │               (独立服务名)                            │                  │
  │  POST /video_cso_exec_engine/            │                      │                      │                  │
  │  api/flow/core/          │                      │                      │                  │
  │  warning                 │                      │                      │                  │
  │  + Bearer Token          │                      │                      │                  │
  │ ────────────────→        │                      │                      │                  │
  │                  │                              │                      │                  │        
  │                  │ 校验 Token                    │                      │                  │
  │                  │ 路径匹配 /core/**              │                      │                  │
  │                  │ Nacos 发现服务                 │                      │                  │
  │                  │ lb://video_cso_exec_engine-flow-engine-core  │                      │                  │
  │                  │                              │                      │                  │        
  │                  │  Feign POST                  │                      │                  │
  │                  │  /video_cso_exec_engine/api/flow/            │                      │                  │
  │                  │  core/warning                │                      │                  │
  │                  │ ───────────────────→         │                      │                  │
  │                  │                      │                              │                  │        
  │                  │                      │ 1. 查路由(Redis缓存)           │                  │
  │                  │                      │   key=core:warning           │                  │
  │                  │                      │ ─────────────────────────────────────→          │
  │                  │                      │ ←─────────────────────────────────────          │
  │                  │                      │                              │                  │        
  │                  │                      │ 2. 加载Schema                 │                  │
  │                  │                      │   (Redis→TaurusDB)              │                  │
  │                  │                      │ ─────────────────────────────────────→          │
  │                  │                      │ ←─────────────────────────────────────          │
  │                  │                      │                              │                  │        
  │                  │                      │ 3. 参数校验 & 组装             │                  │
  │                  │                      │                              │                  │        
  │                  │                      │ 4. POST /api/task/run        │                  │
  │                  │                      │    (sync:true in body)       │                  │
  │                  │                      │ ───────────────────→         │                  │
  │                  │                      │                      │ 执行 FlowGram             │
  │                  │                      │                      │ runtime-js               │
  │                  │                      │                      │                          │        
  │                  │                      │ 5. 返回执行结果                │                  │
  │                  │                      │ ←───────────────────         │                  │
  │                  │                      │                              │                  │        
  │                  │                      │ 6. 记录执行日志                │                  │
  │                  │                      │   (含 service_tier)          │                  │
  │                  │                      │ ─────────────────────────────────────→          │
  │                  │                      │                              │                  │        
  │                  │   统一响应                    │                      │                  │
  │                  │ ←───────────────────         │                      │                  │
  │                  │                              │                      │                  │        
  │   响应                                                                                     │
  │ ←────────────────        │                      │                      │                  │
```

## 附录 B：flowCode 编码规范

| 规则 | 说明 | 示例 |
|------|------|------|
| 字符集 | 小写字母 + 数字 + 短横线 | `warning`、`device-query` |
| 长度 | 3 ~ 64 字符 | |
| 不允许 | 大写、下划线、中文、特殊字符 | |
| 建议命名 | `{业务域}-{动作}` 或 `{业务域}-{对象}-{动作}` | `alarm-list`、`device-bindstatus-query` |

## 附录 C：参数类型映射

| 推断类型 | Java 类型 | JSON Schema type | 说明 |
|---------|----------|-----------------|------|
| String | String | string | 默认类型 |
| Integer | Integer | integer | 32 位整数 |
| Long | Long | integer | 64 位整数 |
| Double | Double | number | 浮点数 |
| Boolean | Boolean | boolean | 布尔值 |
| Object | Map<String, Object> | object | 嵌套对象 |
| Array | List<Object> | array | 数组 |

## 附录 D：错误排查指引

| 现象 | 可能原因 | 排查步骤 |
|------|---------|---------|
| 404 能力不存在 | flowCode 错误或已下架 | 检查 flow_route 表 status 字段 |
| 400 参数校验失败 | 必填参数缺失或类型不匹配 | 查看响应 msg 中的具体字段 |
| 408 执行超时 | 工作流内节点耗时过长 | 查看 task_executions 中 node_logs |
| 502 上游服务异常 | agents-server 不可用 | 检查 agents-server 健康状态 |
| 401 认证失败 | 凭证无效或签名错误 | 检查 Token/AccessKey 有效性、签名算法 |

## 附录 E：功能与底层技术映射

| 功能 | 工作流编排 能力 |
|------|------------------|
| 自由布局画布、节点、连线 | @工作流编排/free-layout-editor + 插件体系 |
| 节点表单、侧边栏 | form-materials、panel-manager-plugin |
| 变量 | variableEngine、VariablePanelPlugin |
| 运行与轮询 | runtime-js 的 TaskRunAPI/ReportAPI/ResultAPI/CancelAPI/ValidateAPI |
| 执行记录 | 自研 agents-server + TaurusDB（信创：达梦/金仓） |
| **流程发布** | **自研 smart-flow-engine (Spring Boot)** |
| **动态路由** | **统一 Controller + 路由表 + Redis 缓存（信创：TongRDS）** |
| **双网关接入** | **Nacos 注册 + Feign 调用** |
| **信创适配** | **Profile 多环境 + ORM 方言 + 缓存抽象 + DDL 多版本（定开）** |

## 附录 F：文件/接口索引

| 功能域 | 前端入口 | 后端接口 |
|--------|---------|---------|
| 智能体管理 | `/manage` | `GET/POST/PUT/DELETE /api/agents` |
| 工作流编辑 | `/editor/:id` | `GET/PUT /api/agents/:id` |
| 任务执行 | 编辑器运行按钮 | `POST /api/task/run`、`GET /api/task/report`、`GET /api/task/result` |
| **流程发布** | 编辑器「发布」 | `POST /video_cso_exec_engine/api/publish`、`PUT /video_cso_exec_engine/api/publish/:id/unpublish` |
| **动态路由调用** | 能力市场调试 | `POST /video_cso_exec_engine/api/flow/{serviceTier}/{flowCode}` |
| **能力市场** | `/marketplace` | `GET /video_cso_exec_engine/api/marketplace/list`、`GET /video_cso_exec_engine/api/marketplace/:flowCode` |
| 运行日志 | 管理页弹窗 | `GET /api/agents/:id/executions` |

---

> **文档说明**: 本文档整合了工作流编排工具的功能架构、技术架构、对外调用方案与信创兼容适配方案，形成调度服务引擎的总体建设方案。随项目演进可持续迭代更新。
