## Cursor Cloud specific instructions

This repository is currently an empty skeleton — it contains only a `readme.md` file. There is no application code, no package manager configuration, no build system, no CI/CD, and no services.

- **No dependencies** to install (no `package.json`, `requirements.txt`, `go.mod`, etc.)
- **No lint, test, or build commands** are available.
- **No services** need to be started.

When application code is added in the future, this file should be updated with relevant development instructions.
